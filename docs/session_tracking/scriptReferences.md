# Script References

**Quick reference for all Python scripts in the Psalms AI Analysis project.**  
Always read this file first when you need to find code.

---

## Pipeline Runners (`scripts/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `run_enhanced_pipeline.py` | `scripts` | **NEW DEFAULT**: Master Writer pipeline. Single-pass generation from research inputs. Questions and insights skipped by default (`--include-questions`/`--include-insights` to opt in). Citation verifier runs with GPT-5.1 filter by default (`--no-gpt-filter` to disable). Use `--skip-copy-editor` to skip QA output. `--exclude` flags omit existing files entirely. Session 338: added STEP 1b literary echoes generation (default on, regenerate-and-overwrite), `--skip-lit-echoes` flag. **Session 347**: STEP 3.5 cross-verse synthesis-discovery pass between micro and writer (`src/agents/synthesis_discovery.py`; now default ON, `--skip-synthesis-discovery` to disable) — saves observations to `output/psalm_NNN/psalm_NNN_synthesis_discovery.md` and splices them into the writer prompt as additional input. Sidecar — does NOT structure the commentary. Adds ~$2/psalm. **Session 358**: `--reuse-synthesis-discovery` reuses an existing observations file instead of regenerating (~$2 saved; writer still receives it) — for writer-only reruns. **Session 367**: `--gpt-5-4-*` flags (`-all/-macro/-insight/-question/-copy/-writer`) are pin-back-to-gpt-5.4 escape hatches now that the GPT default is `gpt-5.6-terra`. **Session 368**: `copy_mdl` reads `CopyEditor.DEFAULT_MODEL` (= `gpt-5.4`) instead of hardcoding Terra — the copy editor is deliberately the one GPT agent off Terra, see `docs/plans/COPY_EDITOR_TERRA_FINDINGS.md`. **Session 373 — THE WRITER IS NOW `claude-opus-5`.** `--master-editor-model` had defaulted to `claude-opus-4-8` since the flag existed, while every writer-prompt change from Session 370 on was A/B'd on Opus 5 (`ab_writer_prompts.DEFAULT_MODEL`); Ps 72 shipped arm E's prompt on 4.8 as a result. Do not "restore" 4.8. **Synthesis discovery no longer follows the writer**: `sd_model` used to read `master_editor_model if "claude" in ...`, so this flip would have dragged the sidecar to Opus 5 too — it is now pinned to `synthesis_discovery.DEFAULT_MODEL` (Opus 4.8, author's call for cost) with a new `--synthesis-discovery-model` override. Macro stays 4.8 and has never been A/B'd on Opus 5. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_enhanced_pipeline.py) |
| `run_enhanced_pipeline_TEST.py` | `scripts` | **PHASE 1 TEST**: V3 Prompt Overhaul test pipeline. Suffixes output with `_TEST`. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_enhanced_pipeline_TEST.py) |
| `run_enhanced_pipeline_with_synthesis.py` | `scripts` | **LEGACY**: Original Synthesis Writer pipeline (Pass 3 Synthesis → Pass 4 Master Editor). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_enhanced_pipeline_with_synthesis.py) |
| `run_docx_only.py` | `scripts` | **UTILITY**: Fast DOCX layout text generation without hitting any agents. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_docx_only.py) |
| `run_si_pipeline.py` | `scripts` | **NEW SI**: Special Instruction pipeline using Master Writer approach. Matches enhanced pipeline controls (including copy editor, GPT-5.1 citation filter). Session 338: added STEP 1b literary echoes generation, `--skip-lit-echoes` flag. **Session 368**: `copy_mdl` reads `CopyEditor.DEFAULT_MODEL` (= `gpt-5.4`) instead of hardcoding `gpt-5.6-terra`, same as the enhanced pipeline — see `docs/plans/COPY_EDITOR_TERRA_FINDINGS.md`. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_si_pipeline.py) |
| `run_literary_echoes.py` | `scripts` | **NEW (Session 338)**: Standalone runner for the 4-pass literary echoes workflow (Gemini 3.1 Pro × 2 → GPT-5.4 verify w/ web search → GPT-5.4 reconstruct). Default behavior is regenerate-and-overwrite; `--skip-if-exists` preserves existing file. Prints per-pass cost breakdown at completion. ~$0.95/psalm, ~10 min. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_literary_echoes.py) |
| `run_si_pipeline_with_synthesis.py` | `scripts` | **LEGACY SI**: Special Instruction pipeline using Synthesis Writer approach. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_si_pipeline_with_synthesis.py) |
| `run_copy_editor.py` | `scripts` | Standalone copy editor runner. Applies 10-category error taxonomy to existing `print_ready.md`. Supports batch (`36 37 38`), `--dry-run`, `--model`, `--input-file`, `--output-dir`. **Session 368**: default model is `gpt-5.4`, NOT the Terra default used elsewhere — see `docs/plans/COPY_EDITOR_TERRA_FINDINGS.md` before changing it. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_copy_editor.py) |
| `run_beta_reader.py` | `scripts` | **NEW (Session 362)**: Standalone beta-reader runner. Simulated target-audience read of the finished guide (`copy_edited.md`, fallback `print_ready.md`) producing a reader-EXPERIENCE report — engagement curve, quoted AHA/WIT/FELT moments, affective-landing check, sag points, confusion, 5 parsed scores. **Session 370** added two measurement sections at no extra API cost: `5b` emits `INERT CITATIONS: N` (commentator quotations that taught the reader nothing, judged on what the commentator *says* rather than how the guide introduces it) and `5c` emits `UNEXPLAINED GRAMMAR: N` (grammar terms used without showing what they mean). **Session 372 REVERSED the Session-370 claim that these are 'the trustworthy signal': on a FIXED text, four re-reads move `UNEXPLAINED GRAMMAR` by up to 6 and `INERT CITATIONS`/`Wit` by up to 3, and Session 371's arm-A grammar finding INVERTS under repeated measurement.** The beta reader is consequently **OFF by default in both production pipelines** (`--beta-reader` opts in; `--skip-beta-reader` retained as a no-op). Use it for a deliberate one-off read, never as A/B evidence at n=1. Session 372 also added `5d` emitting `POET'S FEELING: N` (the POET's own conveyed emotion, distinct from the reader's AHA) — which showed NO discrimination (0-2 across all arms) and needs redesign. Measurement-only (no edits). Batch psalms, `--input-file`, `--model` (default `claude-sonnet-4-6`, ~$0.08/psalm). Output: `psalm_NNN_beta_read.md`. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_beta_reader.py) |
| `run_scripture_verifier.py` | `scripts` | Standalone scripture citation verifier. Checks quoted Hebrew against `tanakh.db`. GPT-5.1 false-positive filter runs by default (`--no-gpt-filter` to disable, `--haiku-filter` for cheaper alternative). Supports batch psalms, `--fix` for copy-editor fix pass, `--tooluse-verify` for Haiku tool-use supplementary check (~$0.04/psalm). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_scripture_verifier.py) |
| `ab_writer_models.py` | `scripts` | **NEW (Session 367)**: Writer-only model A/B. Re-reads a completed runs macro / micro / research_v2 / synthesis_discovery artifacts and runs ONLY `MasterEditor.write_commentary` once per model, so the model is the sole variable (all expensive shared upstream is computed once by a normal pipeline run). Per-arm CostTracker; writes to gitignored `output/psalm_N/_writer_ab/<model>/`; emits `ab_summary.json`/`.md` with cost, in/out tokens, word counts, wall time and ratios. `--models` (default `claude-opus-4-8 claude-opus-5`), `--beta-read` (~/usr/bin/bash.08/arm). One arm failing never discards the other arms paid output. Used for the Session-367 Opus 5 decision. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/ab_writer_models.py) |
| `ab_finish_arms.py` | `scripts` | **NEW (Session 367)**: Takes each `_writer_ab` arm through the rest of the pipeline so the models can be compared as finished Word documents — mirrors run_enhanced_pipeline STEP 5 (print-ready) → 5a½ (citation verification + GPT-5.1 FP filter) → 5b (copy editor) → 5c (section re-extraction, preserving `*_pre_copy_edit.md`) → STEP 6 (DOCX). Every step is pointed at the arms own directory, so production files are never touched; DOCX filenames carry a model label (`Psalm 70 (Opus 5).docx`). `--skip-verify`, `--arms`, `--copy-to-documents`, `--ab-dir` and `--writer-model` (**Session 371** — prompt-variant arms live in `_prompt_ab` and their directory name is an ARM ID, not a model, so without `--writer-model claude-opus-5` the methodology block would credit the commentary to `A_no_scaffolding`; same bug class as the Session-368 fix below), `--copy-editor-model` (**Session 368** — it previously constructed `CopyEditor()` with no model, silently pinning both arms to `DEFAULT_MODEL` regardless of any pipeline flag). **Session 368 fix**: `_arm_stats_file()` now writes a per-arm `pipeline_stats.json` with `model_usage.master_writer` set to the arm's model — previously both arms were handed the production stats, so every DOCX methodology block credited the production writer (both Ps 70 and Ps 71 Opus-5 documents read "Master Writer: claude-opus-4-8"). Both the formatter and `DocumentGenerator` read that file independently; both now take the patched copy. ~$0.35–0.85/arm. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/ab_finish_arms.py) |
| `ab_writer_prompts.py` | `scripts` | **NEW (Session 371)**: Sibling of `ab_writer_models.py` — model FIXED (default `claude-opus-5`), PROMPT varies. Re-reads a completed run's macro / micro / research_v2 / synthesis_discovery and runs only `MasterEditor.write_commentary` once per arm, monkeypatching `master_editor.MASTER_WRITER_PROMPT_V4` for the call and restoring it in `finally` (a leaked patch would silently contaminate later arms). Arms defined in `writer_prompt_variants.py`. Every arm's prompt is BUILT AND VALIDATED BEFORE ANY SPEND, so a stale anchor fails for free; `--dry-run` prints each arm's char delta. Writes to `output/psalm_N/_prompt_ab/<arm>/` plus a per-arm `_prompt_template.txt` audit copy; `ab_summary.json` MERGES across invocations so arms run separately accumulate. `--arms` (required), `--model`, `--beta-read`. ~$2.1/arm (the writer call carries ~205K input tokens — Session 370's $0.6–1 estimate was wrong). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/ab_writer_prompts.py) |
| `writer_prompt_variants.py` | `scripts` | **NEW (Session 371)**: Prompt arms as pure `str -> str` transforms over `MASTER_WRITER_PROMPT_V4`, plus `LABELS` (also merged into `ab_finish_arms.ARM_LABELS`). `variant_a` deletes the verification scaffolding (FINAL VALIDATION CHECKLIST + 2 inline VALIDATION CHECK blocks) but deliberately KEEPS RULE 11, PHRASE COVERAGE and RULE 8b's flat-gloss test — those are rules phrased as questions, not re-read passes. `variant_b` inserts six `WORKED EXAMPLES` blocks (RULE 7, 7b, 8, 8b, 3b-2, affective landing), add-only, each carrying RULE 13's anti-pastiche guard. `variant_c` adds a conciseness instruction (REJECTED — backfired). Every transform asserts its anchors are present and UNIQUE before editing: a silent no-op would cost a real ~$2.1 call and masquerade as a null result. **Label caveat**: labels are interpolated as `Psalm <n> (<label>).docx`, so a label must not repeat the psalm number. **Session 372-373: arms B, D and E are now IN PRODUCTION, so `variant_b`/`variant_d`/`variant_e` are no-ops that RAISE during pre-flight** — that is the intended behaviour, not a bug; build new arms as deltas from production. `variant_d` deletes the verification scaffolding AND the `300-500 words per verse` target (the real length driver, unvaried since Session ~130); `variant_e` adds the author's offset TRANSLATION SLOT and deletes what it replaces (PHRASE COVERAGE, the word-and-phrase floor, the whole-verse sweep example). **Arm B's exemplar text must be kept byte-identical with `master_editor.py`** — the blocks are recognised as "already present" by exact match, so editing one copy and not the other makes `variant_b` silently insert a stale duplicate. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/writer_prompt_variants.py) |
| `score_prompt_ab.py` | `scripts` | **NEW (Session 371)**: Scores `_prompt_ab` (or any `--ab-dir`) arms into a comparison table + `scorecard.md`. Reads each arm's `psalm_NNN_full.md` — the RAW writer output — NOT `_edited_verses.md`, which after `ab_finish_arms` holds COPY-EDITED text and would measure the copy editor as much as the prompt. Metrics: verse/intro words, words-per-verse, commentator mentions, Tier-1 quotes (a commentator named with ≥4 Hebrew words within 250 chars) and per-verse rate, commentator density, grammar terms named (RULE 3b-2's own list), bolded Hebrew, blurry nouns (RULE 7's own list), redundancy markers, blockquote lines, plus the beta reader's `INERT CITATIONS` / `UNEXPLAINED GRAMMAR` / `POET'S FEELING` / `LANDING` — **which Session 372 marked `†` with their measured noise bands after calibration showed them unusable at n=1; read the deterministic rows instead**. Every other metric is a structural count, never a match against a prior arm's wording — see `WRITER_PROMPT_RULE_8B_FINDINGS.md` §7, where wording-keyed canaries produced four false "lost" verdicts. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/score_prompt_ab.py) |
| `check_phrase_coverage.py` | `scripts` | **NEW (Session 371)**: Audits the author's standing requirement that every word of every verse be treated at least briefly — the gate any length-reducing change must pass. Ground truth is `tanakh.db` (not the guide's own printed verse line, so an incompletely printed verse still counts every word). Compares CONSONANTAL SKELETONS: cantillation, vowels and maqqef stripped, and **markdown emphasis deleted first** (RULE 3b-2 bolds a prefix inside a word, which would otherwise split the token and penalise the arm that bolds most). Inseparable prefixes (והבכלמש, up to two) are stripped from the target, since a guide discussing בְכִנּוֹר under the bare noun כִּנּוֹר has covered it. The printed verse line opening each block is excluded. `--arm`, `--detail`, `--ab-dir`. **Limitation: measures Hebrew re-quotation, not conceptual treatment** — a word glossed only in English scores as uncovered, so absolute values understate every arm equally and the RANKING is the finding. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/check_phrase_coverage.py) |
| `check_banned_phrases.py` | `scripts` | **NEW (Session 374)**: Audits the finished guide for house-style vocabulary the author has ruled out — seeded with **"load-bearing"**, which he flagged as a verbal tic of contemporary Claude models (measured: 18 occurrences across 10 of the 84 delivered guides, including the two most recent runs). The list lives in `src/utils/banned_phrases.py` and is SHARED with the copy editor, which renders it as error category 11 — one source of truth, so the rule the model is given and the list audited against cannot drift. **Reports only, never rewrites**: the right repair differs per site and two or three instances are live architectural metaphors (Ps 8's foundations argument from Prov 8:29, Ps 66's "load-bearing wall of the whole edifice") that a swapped synonym would wreck. Reads psalm numbers (resolving `_copy_edited.md`, falling back to `_print_ready.md`), explicit `.md`/`.docx` paths, or `--delivered` for every guide in `Documents/Psalm study guide/`. Exit 1 on any hit, so it can gate a run. `--quiet` prints only files with hits. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/check_banned_phrases.py) |
| `beta_reader_variance.py` | `scripts` | **NEW (Session 372)**: Measuring-instrument calibration — re-reads ONE FIXED text N times and reports the within-text spread of every beta-reader score and counter. Answers the question nobody had asked in three sessions of prompt A/B work: is the between-arm difference larger than the judge's own noise? Answer, on Ps 71: **no**, for `INERT CITATIONS` (range 3), `UNEXPLAINED GRAMMAR` (range 5-6), `Wit` (range 3) and `Emotional impact` (range 2). Only `Engagement`/`Freshness` are stable, and only the A-vs-base wit gap exceeds the noise. Writes repeat reports to `<ab-dir>/_variance/<arm>/runN/`, **never over an arm's real beta read**. `--arms` (required), `--repeats` (default 4), `--out`. ~$0.08 per read. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/beta_reader_variance.py) |
| `EXPERIMENT_micro_terra_probe.py` | `scripts` | **EXPERIMENTAL (Session 367) — archive after use**. Answers "is gpt-5.6-terra as good as claude-sonnet-4-6 at Hebrew close reading?" WITHOUT building an OpenAI branch in the Anthropic-only `micro_analyst.py`. Rebuilds the exact Stage 1 `DISCOVERY_PASS_PROMPT` (same macro markdown, LXX-formatted psalm text, RAG context), sends it to Terra at `reasoning_effort=xhigh` (Terra rejects `max`), and diffs the parsed result against the saved `micro_v2.json`. Schema-agnostic comparison (raw Stage 1 emits `verse_discoveries`/`observations`; `_create_micro_analysis` renames to `verse_commentaries`/`commentary`). NOTE `--db-path` defaults to `database/tanakh.db` — the class default `data/tanakh.db` is a 69KB stub. ~/usr/bin/bash.54/psalm. Verdict: REJECTED (half the prose per verse). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/EXPERIMENT_micro_terra_probe.py) |
| `EXPERIMENT_two_call_synthesis.py` | `scripts` | **EXPERIMENTAL (Session 346) — keep for Session 347 reference, then archive**. Two-call synthesis experiment for the Master Writer. Reuses the saved production writer prompt (`output/debug/master_writer_v4_prompt_psalm_NNN.txt`) split at the `## YOUR TASK` marker; Call 1 = synthesis discovery (brainstorm + adversarial novelty filter + evidence-honesty filter, no prose); Call 2 = write using approved spine as anchor-verse skeleton. Both calls `claude-opus-4-7` (effort=max). Retry/resume hardening included. Writes only to `output/psalm_NNN/EXPERIMENT_two_call/`. Discardable — touches no production code. Session 347 plan supersedes the spine approach but the `SYNTHESIS_TASK` prompt (steps 1, 2, 2b) is the template to reuse for the new `synthesis_discovery` agent. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/EXPERIMENT_two_call_synthesis.py) |
| `EXPERIMENT_two_call_finalize.py` | `scripts` | **EXPERIMENTAL (Session 346) — keep for Session 347 reference, then archive**. Runs experiment output through the production downstream chain (print-ready → citation verifier with GPT-5.1 FP filter → copy editor at gpt-5.4 → section extraction → DOCX) with all writes isolated to `output/psalm_NNN/EXPERIMENT_two_call/`. Useful template for any future experiment that needs the production downstream chain without clobbering shipped files. Deliberately does NOT call `run_scripture_verifier.py` or `run_docx_only.py`, both of which hardcode production output paths. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/EXPERIMENT_two_call_finalize.py) |
| `EXPERIMENT_concordance_eval.py` | `scripts` | **EXPERIMENTAL (Session 350) — discardable, keep for D/E validation then archive**. Re-runs only the concordance selection + retrieval path for a set of psalms, reusing production Stage-1 discoveries from each `micro_v2.json` (no re-discovery). Runs modified Stage-2 (live sonnet) → librarian, reports per-search external yield vs the Session-349 baseline. Used to measure the 24%→~90% improvement. Usage: `python scripts/EXPERIMENT_concordance_eval.py 54 55 56 57 58`. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/EXPERIMENT_concordance_eval.py) |
| `EXPERIMENT_concordance_trace.py` | `scripts` | **EXPERIMENTAL (Session 350) — discardable**. Traces ONE psalm's concordance pipeline end to end via lightweight wrappers on the real production methods: STAGE A (raw LLM picks) → STAGE B (after `_override_llm_base_forms`) → STAGE C (after `_augment_with_root_searches`) → FINAL (searched + results). Usage: `python scripts/EXPERIMENT_concordance_trace.py 55`. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/EXPERIMENT_concordance_trace.py) |
| `converse_with_editor.py` | `scripts` | Enables multi-turn conversation with Master Editor about a completed psalm commentary with full research context. Supports multi-line copy/pasting and interactive model selection (Claude, Gemini, GPT). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/converse_with_editor.py) |
| `concordance_tool.py` | `scripts` | Interactive Hebrew concordance CLI. 4 match modes (exact, variations, substring, substring + AI filter), lexicon lookup (BDB/Klein), AI commentary, cost tracking, markdown export. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/concordance_tool.py) |
| `build_stress_lexicon.py` | `scripts` | **DATA BUILDER (Session 369)**: regenerates `src/agents/stress_lexicon.json.gz` from `database/tanakh.db`. For every pointed form, records the stressed letter where the cantillation settles it unambiguously, so `phonetic_analyst.py` can resolve words whose own accent gives no position (dehi/geresh muqdam/ole are prepositive; a lone postpositive gives nothing either). Only records stresses the analyst reads off a real accent, so a guess can never feed back in; drops the 122 forms whose readings disagree below 60% (pausal retraction), leaving those to the accents. Deterministic, $0, ~2 min. `--check` reports without writing; verifies the round-trip through the analyst's loader. Re-run if `tanakh.db` or the accent classification changes. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/build_stress_lexicon.py) |
| `add_lemma_column.py` | `scripts` | **ONE-OFF MIGRATION (Session 351)**: adds and populates the `lemma` column on `concordance` from ETCBC/BHSA `lex_utf8`. Greedy concat-alignment of BHSA word tokens to our (proclitic-attached) tokens — handles BHSA splitting ב/כ/ל/מ/ו/ה into separate nodes — assigns each token its content lemma; exact-match-required so misses stay NULL (→ naive fallback), never wrong. 96.3% coverage, builds `idx_concordance_lemma`. `--dry-run` reports per-book coverage without writing; `--book Psalms` restricts. Requires `text-fabric` + the cached BHSA 2021 data. Re-runnable (idempotent). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/add_lemma_column.py) |
| `run_synthesis_ab.py` | `scripts` | **A/B HARNESS (Session 357) — keep for prompt-iteration reruns**. Runs the synthesis-discovery v1 (as of S347-356) and v2 "Synthesis Scholar" prompts head-to-head over byte-identical Step-3.5 inputs (reuses `MasterEditorSI` loaders + research trimmer + the agent's streaming call; accepts non-padded `output/psalm_N` dirs). Up-front Special-Instruction contamination scan (Ps 67 favoritism canary). Writes `output/ab_synthesis/psalm_NNN_{v1,v2}.md`, exact prompts, scan + summary. Flags: `--only v1|v2`, `--model`, `--out`. ~$4-6/psalm both arms. Requires local dossiers + ANTHROPIC_API_KEY. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_synthesis_ab.py) |
| `synthesis_ab_prompts.py` | `scripts` | **REFERENCE (Session 357)**: the v2 "Synthesis Scholar" prompt exactly as blind-A/B-tested on Pss 67/60/49, preserved verbatim for reproducibility (the harness imports it). ADOPTED to production in `src/agents/synthesis_discovery.py`, which adds one instruction (reconstruct-and-diff reworked source formulas so omissions surface). Not a runner. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/synthesis_ab_prompts.py) |
| `run_novelty_ab.py` | `scripts` | **A/B HARNESS (Session 358) — keep for future pipeline A/Bs**: re-runs psalms through the production `run_enhanced_pipeline.py` (identical harness/defaults) into isolated `output/ab_novelty*/psalm_N` arm dirs so existing outputs are never touched. Full mode (~$5.5-6/psalm): holds macro constant (copies baseline `psalm_0NN_macro.json` + `--skip-macro`) and reuses the existing literary-echoes file. `--reuse-upstream ROOT` (~$3.7/psalm): copies macro/micro/research/synthesis-discovery byte-identical from a prior arm and reruns only writer→verifier→copy-editor→DOCX; add `--regen-synthesis` (~$5.7/psalm) to regenerate the sidecar too (tests sidecar prompt changes). Branch guard (`--allow-any-branch`), `--dry-run`, `--fresh-macro`, `--with-lit-echoes`, `--delay`, `--out-root`. Full 3-round experimental record: `docs/plans/NOVELTY_AB_TEST_RUNBOOK.md`. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/run_novelty_ab.py) |
| `evaluate_novelty_ab.py` | `scripts` | **JUDGE (Session 358) — POSITION-DEBIASED**: blind LLM judge + deterministic diff for FULL-COMMENTARY A/B comparisons (generic old-vs-new). **`--order both` (default): judges each psalm TWICE (old-first and new-first) and emits a position-debiased synthesis** — averaged best-5/gold/bridging/overreach, per-order winners, AGREE/SPLIT flag, winner at ≥0.3 debiased best-5 margin else TIE. This is mandatory for verdicts: a Session-358 control showed the judge favored the second-presented commentary in **12/12** judgments (~+1-2 best-5 pts); judge noise on identical text ≈ ±1.0 best-5. Per psalm also: deterministic metrics (sizes, hedge counts, copy-editor change-log stats incl. the separate `*_copy_edit_changes.md`, citation issues, cost.json). Rubric: insight inventory (NOV/GRD/AHA 1-5, INTERNAL vs BRIDGING, GOLD/GOOD/FILLER), overreach/false-profundity/lost-hedges audit, verse-coverage spot-check. OLD arm resolves from `output/psalm_N` or falls back to the published DOCX; templates via `--old-dir`/`--new-dir`. `--skip-judge` for $0 metrics-only; single-order modes for cheap looks only. ~$2-3/psalm both-orders. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/evaluate_novelty_ab.py) |
| `judge_synthesis_ab.py` | `scripts` | **JUDGE (Session 357)**: blind LLM judge for `run_synthesis_ab.py` outputs. Shuffles arms into Set A/B (seeded; judge never sees the mapping), scores every observation 1-5 on linkage-novelty / aha / economy / groundedness, tags INTERNAL vs BRIDGING + KEEP/MARGINAL/CUT, runs the two Ps 67 canary checks on both sets, compares sets (best-3 mean, keeps, bridging keeps), writes per-psalm reports (unblinded at the end) + cross-psalm summary table parsed from the judge's trailing JSON. ~$0.5-1/psalm. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/scripts/judge_synthesis_ab.py) |

---

## Analysis Agents (`src/agents/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `macro_analyst.py` | `src.agents` | Pass 1: Produces chapter-level thesis and structural framework using Claude Opus 4.6 with RAG context. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/macro_analyst.py) |
| `micro_analyst.py` | `src.agents` | Pass 2: Discovery-driven verse-by-verse research using Claude Sonnet 4.6 with adaptive thinking. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/micro_analyst.py) |
| `synthesis_writer.py` | `src.agents` | Pass 3: Final commentary synthesis combining macro thesis, micro discoveries, and research bundle. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/synthesis_writer.py) |
| `master_editor.py` | `src.agents` | Pass 4: Unified Master Writer V4 — single prompt replacing Main + College. Supports **Opus 4.8** (default) and **Opus 5** with streaming for long generations. **Session 370** added two rules and rescoped a third: **RULE 8b (The Commentator's Burden)** — the dossier supplies all available commentators on every verse unranked, and much of it is paraphrase by design, so a quotation is admitted only if *the reader now reads the verse differently*, cut by default, tested against the gloss rather than the framing verb, never two commentators for one point, ~1 full quotation per verse as a **ranking** budget (protect Torah Temimah/Talmudic material; keep the insight and drop the citation without refilling the room). **RULE 3b-2 (Point, Don't Name)** — assume noun/verb/subject/object/tenses and nothing more; bold the letters or name them in plain words; bolding does not license keeping the term. Line 343's "quote liberally" now exempts commentary to RULE 8b. Bold inside a phonetic transcription is the writer's, for sound arguments — stress arrives as CAPS. **Read `docs/plans/WRITER_PROMPT_RULE_8B_FINDINGS.md` before editing RULE 8b** (records an amendment that backfired). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/master_editor.py) |
| `master_editor_v3.py` | `src.agents` | **PHASE 1 TEST**: V3 Prompt Overhaul variant. Implements 9 major prompt changes. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/master_editor_v3.py) |
| `master_editor_si.py` | `src.agents` | Master Writer V4 with Special Instructions that supports author directives for alternative versions. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/master_editor_si.py) |
| `master_editor_old.py` | `src.agents` | Legacy Master Editor implementation (archived). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/master_editor_old.py) |
| `phonetic_analyst.py` | `src.agents` | Transcribes Hebrew text into phonetic/syllabic structure based on reconstructed Biblical Hebrew phonology. Deterministic, $0; output lands in `micro_v2.json` as `phonetic_transcription` and is quoted verbatim by the writer. **Session 369 rewrite** (author spotted two wrong stresses in the Ps 70 Opus-5 guide): stress placement went 96.85% → 99.54% against the Masoretic text, and 30% of all word transcriptions changed. Accents are now classified by *measured* position reliability, not by name — prepositive (dehi/geresh muqdam/ole) and postpositive (pashta/segolta/telisha qetana/zinor) glyphs sit at a word edge and are not evidence; a doubled accent's inner copy is; U+05BD is meteg (not stress) except as verse-final silluq; U+0598 is the tsinnorit helper unless its partner is a zinor. Also fixed: hiriq/tsere/segol + yod is one long vowel, word-final consonant clusters and shewas no longer spawn phantom syllables, dagesh forte actually geminates (the begadkefat branch was unreachable), mappiq and word-initial dagesh do not, shewa-after-shewa is vocal, `{פ}` markers and paseq no longer become words. `stress_source` on each word says whether the stress was read off an accent, recovered, looked up, or guessed. Tests: `tests/test_phonetic_analyst.py` (30 cases, one per bug class). **Session 370**: stress is now marked with **CAPS only, no markdown bold** (`yā-SHŪ-vū`) — bold inside a transcription belongs to the writer, for the sound it is arguing about (a rhyme, a sibilant, a shared consonant); marking stress with it too would make the two uses indistinguishable on the page. Caps carry the stress unchanged. Takes effect on the next micro run; existing `micro_v2.json` still hold the old `**` markers. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/phonetic_analyst.py) |
| `stress_lexicon.json.gz` | `src.agents` | **DATA (Session 369)**: 48,721 entries, `{pointed form: stressed letter index}`, 0.29 MB. Consulted by `phonetic_analyst.py` only where the accents give no position, so it can never override an accent that is present; covers 8.0% of Psalms words and leaves 1.4% on the ultima guess. Letter indices, not syllable indices, so it survives changes to the syllabifier. Regenerate with `scripts/build_stress_lexicon.py`. | — |
| `copy_editor.py` | `src.agents` | Post-generation QA: 10-category error taxonomy (structural claims, inconsistencies, form/content confusion, negative citations, Hebrew script, weak parallels, factual/textual accuracy, grammar bloat, strained arguments, unexplained technical terms). gpt-5.4. **Session 373 fixed a silent text-corruption bug in `_strip_quotes_around_source_text`**: it strips quotes that WRAP a pure Hebrew/Greek span, but a Hebrew abbreviation's gershayim (ג"כ, מה', הקב"ה) read as an opening quote and paired with the REAL opening quote of the English translation after it, deleting both and reporting no change. Over all 114 pre-copy-edit files, 25 of 220 strips were source-language marks, not quotes — including Sappho's Greek elisions in Ps 55. Guarded by two lookarounds over the whole source-language range INCLUDING nikud; an abbreviation mark abuts its letter, a wrapping quote never does. Forward-only: existing markdown keeps its damage. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/copy_editor.py) |
| `literary_echoes_agent.py` | `src.agents` | **NEW (Session 338)**: 4-pass literary echoes orchestrator. Pass 1+2: Gemini 3.1 Pro (generation + gap-fill). Pass 3: GPT-5.4 Responses API with `web_search_preview` tool (verification with real URLs). Pass 4: GPT-5.4 Chat Completions (mechanical reconstruction — uses 5.4 not 5.1 because 5.1 self-terminated early). Builds rolling exclusion list from the 4 most-recently-rendered files by mtime; injects into both Pass 1 and Pass 2 prompts. Saves per-pass artifacts + exact prompts + cost report under `output/psalm_NNN/literary_echoes/`. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/literary_echoes_agent.py) |
| `synthesis_discovery.py` | `src.agents` | **NEW (Session 347)**: Cross-verse synthesis-DISCOVERY sidecar agent (Opus 4.8, high effort, adaptive thinking, 64K max_tokens, streaming with retry on transient drops — mirrors writer config for evidence parity). Reads the same INPUT BLOCKS the Master Writer sees (psalm text, macro, micro, research bundle, phonetics, framework) and produces a calibrated list of cross-verse OBSERVATIONS (1 governing + N core + N additional). Hardened evidence-honesty filter: 9 named failure modes (homophony-vs-overlap, echo-vs-verbatim, primary-lexical-meaning, no-invented-prooftexts, uniqueness-claims-require-checking, count-before-citing, non-sequitur-synthesis, no-signature-root-claims, match-assertion-to-evidence) PLUS a meta-rule that demands the model name two more failure modes it didn't already check. Output bracketed by `---CROSS-VERSE-OBSERVATIONS-START/END---` markers for machine extraction. Used by `MasterEditor.discover_cross_verse_observations()` and spliced into the writer prompt at `### ANALYTICAL FRAMEWORK` anchor when present. Default writer path (no observations file) leaves prompt byte-identical to production. Triggered by `--synthesis-discovery` flag on the pipeline runner. ~$1.83/psalm at Opus 4.7 max effort. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/synthesis_discovery.py) |

---

## Librarian Agents (`src/agents/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `research_assembler.py` | `src.agents` | Coordinates all librarian agents to assemble comprehensive research bundles for psalm analysis. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/research_assembler.py) |
| `bdb_librarian.py` | `src.agents` | Fetches Hebrew lexicon entries (BDB + Klein Dictionary) from Sefaria API. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/bdb_librarian.py) |
| `concordance_librarian.py` | `src.agents` | Searches Hebrew concordance database with automatic phrase variation detection. **Session 350**: drops source-psalm self-matches (`self_match_count`/`only_self` on the bundle) so counts report genuine external parallels; added `tanakh_frequency()`. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/concordance_librarian.py) |
| `figurative_librarian.py` | `src.agents` | Queries figurative language database (Psalms + Pentateuch) with hierarchical tag support. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/figurative_librarian.py) |
| `figurative_curator.py` | `src.agents` | LLM-enhanced agent (GPT-5.4) that curates and synthesizes figurative language insights. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/figurative_curator.py) |
| `liturgical_librarian.py` | `src.agents` | Queries phrase-level liturgical index with intelligent aggregation and optional LLM summarization. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/liturgical_librarian.py) |
| `liturgical_librarian_sefaria.py` | `src.agents` | Alternative liturgical librarian using Sefaria API directly. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/liturgical_librarian_sefaria.py) |
| `commentary_librarian.py` | `src.agents` | Fetches traditional Jewish commentaries from Sefaria API. **Session 373: `COMMENTATORS` is now 11.** Dropped Metzudat David (author's call) — measured over the 71 production guides it was the THIRD most supplied commentator (610 dossier entries) and the LEAST cited (60, a 10% rate against Radak's 70%), because it is running paraphrase by design, exactly what RULE 8b rejects; the full table is recorded in a comment above `COMMENTATORS`. Added five (author's request), all Sefaria index names verified live and coverage measured over 36 verses: **Romemot El** (Alshich — homiletical, on ~100% of verses and long, so the likeliest to crowd a dossier), **Minchat Shai** (Norzi — Masoretic spelling / accents / variants, 58%), **Metzudat Zion** (bare word glossary, 50%, ~65 chars — the Metzudot's GLOSSARY half, a different work from the dropped paraphrase), **Chomat Anakh** (the Chida — eclectic, sparse at 19%), and **Malbim Beur Hamilot** (the Malbim on the WORDS, distinct from the `Malbim` entry on the matter, 36%). None of the five carries English on Sefaria. `COMMENTATOR_DISPLAY` / `display_name()` supply the bibliography labels used in the DOCX methods section, so a reader meets "Chomat Anakh (Chida)" rather than a bare title. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/commentary_librarian.py) |
| `sacks_librarian.py` | `src.agents` | Loads and formats Rabbi Jonathan Sacks' references to Psalms from his collected works. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/sacks_librarian.py) |
| `related_psalms_librarian.py` | `src.agents` | Retrieves related psalms with shared roots, phrases, and skipgrams from top connections analysis. **Session 365**: detects doublets deterministically (consonantal verse-level Jaccard + same-offset gap-fill) and leads with a DOUBLET headline + word-level variant diffs, suppressing atom listings for doublet pairs; shared roots capped at top-10 by IDF (common tail name-only). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/related_psalms_librarian.py) |
| `rag_manager.py` | `src.agents` | Manages RAG documents (analytical framework, psalm functions, Ugaritic parallels) for agent prompts. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/rag_manager.py) |
| `scholar_researcher.py` | `src.agents` | Coordinates research requests by generating specific requests for librarian agents from macro overview. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/scholar_researcher.py) |
| `question_curator.py` | `src.agents` | Generates "Questions for the Reader" from macro/micro analysis using gpt-5.4. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/question_curator.py) |
| `insight_extractor.py` | `src.agents` | **NEW**: Filters research materials for transformative insights using gpt-5.4. Now uses full psalm text and macro context. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/agents/insight_extractor.py) |

---

## Concordance (`src/concordance/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `search.py` | `src.concordance` | Hebrew concordance search with single/multi-word phrase support and multiple normalization levels. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/concordance/search.py) |
| `hebrew_text_processor.py` | `src.concordance` | Text processing utilities for Hebrew normalization and word splitting. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/concordance/hebrew_text_processor.py) |
| `morphology_variations.py` | `src.concordance` | Generates morphological variations (prefixes, suffixes) for Hebrew words. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/concordance/morphology_variations.py) |
| `root_matcher.py` | `src.concordance` | Hebrew root matching utilities for concordance searches. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/concordance/root_matcher.py) |
| `root_selection.py` | `src.concordance` | **NEW (Session 350)**: Picks the single most distinctive content word from a lexical insight (phrase + analyst variants) to trace on its own — "distinctive" = rarest in Tanakh within a frequency window, preferring 3-letter bare roots over rare inflections. Used by `MicroAnalystV2._augment_with_root_searches`. Surface-frequency-based (a deliberate stopgap; true lemma/root selection is the D/E work). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/concordance/root_selection.py) |

---

## Data Sources (`src/data_sources/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `tanakh_database.py` | `src.data_sources` | SQLite database manager for storing biblical texts with concordance indexing. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/data_sources/tanakh_database.py) |
| `sefaria_client.py` | `src.data_sources` | API client for fetching biblical texts and commentaries from Sefaria. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/data_sources/sefaria_client.py) |
| `create_liturgy_db.py` | `src.data_sources` | Creates and populates the liturgy database from source data. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/data_sources/create_liturgy_db.py) |

---

## Hebrew Analysis (`src/hebrew_analysis/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `morphology.py` | `src.hebrew_analysis` | Hebrew morphological analysis utilities. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/hebrew_analysis/morphology.py) |
| `root_extractor_v2.py` | `src.hebrew_analysis` | Extracts Hebrew roots from words. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/hebrew_analysis/root_extractor_v2.py) |
| `word_classifier.py` | `src.hebrew_analysis` | Classifies Hebrew words by type and structure. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/hebrew_analysis/word_classifier.py) |
| `cache_builder.py` | `src.hebrew_analysis` | Builds caches for Hebrew analysis operations. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/hebrew_analysis/cache_builder.py) |
| `demonstrate_expected_results.py` | `src.hebrew_analysis` | Demonstrates expected results for Hebrew analysis features. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/hebrew_analysis/demonstrate_expected_results.py) |

---

## Liturgy (`src/liturgy/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `liturgy_indexer.py` | `src.liturgy` | Indexes liturgical texts for phrase-level searching. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/liturgy/liturgy_indexer.py) |
| `phrase_extractor.py` | `src.liturgy` | Extracts phrases from liturgical texts for indexing. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/liturgy/phrase_extractor.py) |
| `sefaria_json_parser.py` | `src.liturgy` | Parses Sefaria JSON exports for liturgical data. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/liturgy/sefaria_json_parser.py) |
| `sefaria_links_harvester.py` | `src.liturgy` | Harvests links between psalms and liturgical texts from Sefaria. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/liturgy/sefaria_links_harvester.py) |
| `sefaria_liturgy_harvester.py` | `src.liturgy` | Harvests liturgical text content from Sefaria API. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/liturgy/sefaria_liturgy_harvester.py) |
| `sefaria_metadata_scraper.py` | `src.liturgy` | Scrapes metadata about liturgical texts from Sefaria. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/liturgy/sefaria_metadata_scraper.py) |

---

## Thematic / RAG (`src/thematic/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `chunk_schemas.py` | `src.thematic` | Defines schemas for RAG document chunks. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/thematic/chunk_schemas.py) |
| `corpus_builder.py` | `src.thematic` | Builds thematic corpus for RAG retrieval. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/thematic/corpus_builder.py) |
| `embedding_service.py` | `src.thematic` | Embedding generation service for RAG vector search. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/thematic/embedding_service.py) |
| `vector_store.py` | `src.thematic` | Vector store management for RAG similarity search. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/thematic/vector_store.py) |

---

## Utilities (`src/utils/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `model_effort.py` | `src.utils` | **NEW (Session 367)**: Single source of truth for the Anthropic `output_config.effort` level used by the three deep-reasoning Claude agents (Macro Analyst, Synthesis Discovery, Master Writer). Replaces three copy-pasted `if "opus-4-8" in model` ladders in which an unmatched model silently fell through with NO `output_config` and nothing in the logs. `effort_for(model)` / `apply_effort(kwargs, model, logger)`; logs unmatched models. Mapping: 4.7→max, 4.8→high, opus-5→high, else omit (Opus 4.6 and earlier 400 on output_config). | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/model_effort.py) |
| `openai_usage.py` | `src.utils` | **NEW (Session 367)**: Correct reasoning-token extraction for OpenAI usage objects. `usage.reasoning_tokens` does not exist on either API — it is nested under `completion_tokens_details` (chat.completions) or `output_tokens_details` (responses), so six agents silently logged `thinking_tokens: 0` for a long time. **Use `split_output_tokens(usage) -> (non_thinking, reasoning)` at any site feeding CostTracker**: `completion_tokens` already CONTAINS reasoning and CostTracker SUMS output+thinking, so passing both double-bills (+6.9% on Ps 68 volume). The split keeps totals bit-identical while making the reported split correct. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/openai_usage.py) |
| `research_trimmer.py` | `src.utils` | **NEW**: Utility for intelligently trimming research bundles to fit token limits. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/research_trimmer.py) |
| `document_generator.py` | `src.utils` | Generates print-ready Word (.docx) documents from pipeline outputs. **Supports Arabic/Hebrew (RTL) and CJK fonts.** **Session 370 fixed three formatting bugs**: (1) quoted poetry was effectively double-spaced — three code paths set the 0.5" indent but none touched spacing, so `Normal`'s 8pt `space_after` opened a gap between every line; `_apply_quote_format` now gives single spacing and 0pt within a block, 8pt only after the last line, and `_collect_quote_block` absorbs the blank lines markdown puts between poem lines (which previously made each line its own block). (2) `_add_paragraph_with_soft_breaks` split on a literal `$$` instead of `\*\*.*?\*\*`, so `**bold**` was dead in all verse-commentary prose (latent — nothing had bolded there). (3) both `_add_nested_formatting*` split on a bare `**` delimiter, putting the bold on an empty string — **this shipped**, so phonetic stress syllables rendered flat in every guide that quoted one (Psalm 41: 12 empty bold runs, 0 bolded stress marks). Re-rendering affected psalms costs $0. **Session 373 added two more**: (4) `_mirror_bold_to_complex_script()` — `w:b` is the LATIN bold property, so bold on a `w:rtl` (Hebrew) run needs `w:bCs`, which NO delivered DOCX had ever contained; RULE 3b-2's `מִ**כַּ**ף` letter-bolding depended on a Word fallback rather than on correct markup. BOLD ONLY — italic is deliberately NOT mirrored, since Hebrew has no italic form and quote blocks are italicised wholesale. (5) `_split_long_hebrew_block` now absorbs a trailing geresh/gershayim (כולם הם מה') back into the Hebrew block instead of orphaning it onto the head of the English continuation (`', "so Your righteousness…`), guarded so a genuinely WRAPPED span keeps its closing quote outside. Regression over 28,675 production paragraph lines: 2 changes, both improvements. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/document_generator.py) |
| `combined_document_generator.py` | `src.utils` | *(Deprecated V4)* — was Main + College combined doc. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/combined_document_generator.py) |
| `commentary_formatter.py` | `src.utils` | Formats commentary output for various purposes. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/commentary_formatter.py) |
| `divine_names_modifier.py` | `src.utils` | Modifies divine names (YHWH, etc.) according to style preferences. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/divine_names_modifier.py) |
| `cost_tracker.py` | `src.utils` | Tracks API costs across all LLM calls during pipeline execution. **Session 373 audited every `PRICING` row against the vendors' live pages (2026-08-03); 2 of 12 were wrong and both were live in production**: Haiku 4.5 was $0.80/$4.00 (actually **$1.00/$5.00** — under-reported the citation verifier by 20%) and GPT-5.6 Terra was $2.50/$15.00 (actually **$2.00/$12.00** — over-reported ~25%, and Terra is the heaviest non-Anthropic spender: Ps 72 reported $1.3273 vs ~$1.06 true). Terra's stale comment claimed it was "priced IDENTICALLY to gpt-5.4… cost-neutral by construction" — true at its GA, quietly false since. **Re-verify this table when a model is swapped; a wrong row is invisible.** Note the Gemini row encodes only Google's **≤200k-input tier** ($2/$12); above 200k it is $4/$18. Every cost figure in the Session 367–373 record used the two wrong rows. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/cost_tracker.py) |
| `pipeline_summary.py` | `src.utils` | Generates summary statistics and reports for pipeline runs. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/pipeline_summary.py) |
| `scripture_verifier.py` | `src.utils` | Scripture citation verifier. Two modes: (1) **Regex** (default, free): 4 extraction patterns A/B/C/D with normalization, word-level consonantal matching, ellipsis-fragment splitting, optional Haiku FP filter (~$0.003/psalm). (2) **Tool-use** (`verify_citations_tooluse()`, ~$0.04/psalm): Haiku identifies citations via tool-use (`lookup_verse`), Python does programmatic comparison, Haiku filters FPs. Prompt caching reduces multi-turn cost. Pipeline Step 5a½. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/scripture_verifier.py) |
| `logger.py` | `src.utils` | Centralized logging configuration for the project. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/logger.py) |
| `api_guard.py` | `src.utils` | **NEW (Session 342)**: API quota exhaustion detection and pipeline halt utility. `is_quota_exhaustion(exc)` distinguishes permanent billing errors (OpenAI `insufficient_quota`, Anthropic `credit balance too low`, Google `RESOURCE_EXHAUSTED`) from transient rate limits. `halt_on_quota()` saves partial costs, prints halt message, plays audible alert (Windows), exits code 2. Used by both pipeline runners at every non-fatal except block. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/utils/api_guard.py) |

---

## Schemas (`src/schemas/`)

| Script | Namespace | Description | Link |
|--------|-----------|-------------|------|
| `analysis_schemas.py` | `src.schemas` | Pydantic schemas for MacroAnalysis, MicroAnalysis, VerseCommentary, and other data structures. | [file](file:///c:/Users/ariro/OneDrive/Documents/Psalms/src/schemas/analysis_schemas.py) |
