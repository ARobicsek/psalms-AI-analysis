# Psalms AI Commentary Pipeline

**Session**: 364 (2026-07-05)
**Phase**: Pipeline Production — tweaks and improvements

AI-powered system generating scholarly verse-by-verse commentary for all 150 Psalms using Claude (Opus 4.8, Opus 4.7, Opus 4.6, Sonnet 4.6), GPT (5.1, 5.4), and Gemini (2.5 Pro fallback) with multi-agent pipeline and Hebrew concordance integration.

## Recent Work (Last 5 Sessions)
**Session 364 (2026-07-05)**: Local validation of Session-363 features — distributional facts vs real DB (works, 2 fixes), voice_exemplars completed, beta-reader meta-evaluated ($0.76 total)
- **Prompt-era map** (git + mtimes; underpins all findings): Session-359 wit prompt landed 6/21 09:39; local production finals exist for 61–67 (cloud brief was wrong that 61/62 were compare-only). Pre-359: 67, 61, 62. Post-359: 63 (run started 09:42 same morning), 64, 65, 66. All predate the 363 affective-landing prompt → valid "before" baselines.
- **Task 1 — distributional facts WORKS**: `database/tanakh.db` is the real DB (312,479 rows; `data/` copy is a stub). <1s/psalm, ~4K blocks; known facts confirmed (פצמתה Ps 60:4, שבלול Ps 58:9 unique; Ps 58 v.7 YHWH caught); signal real (Ps 108 doublet rows, זנחתנו inclusio, Ps 58 hapax cluster + כמו 6x). **Two fixes shipped**: UTF-8 stdout in `__main__` (cp1252 crash) and **ketiv/qere normalization** (DB stores `(ketiv)` `[qere]` as separate tokens, 2,138 DB-wide → spurious "unique" rows, fake pairs; now reads the qere and counts plain+bracketed together — verified וענני 1x→6x, exits rare table). Sidecar splice self-activates next run.
- **Task 2 — voice_exemplars.md complete**: all 5 known 61/62 candidates verified verbatim in `_writer_compare_new/` (Tier-1 #11–15); 63–66 harvested (#16–22; best: "There is no battle here… **There is a participle.**" Ps 65 v.8; "You cannot photograph an answered prayer."). Wit density post-359 ~4–7 moments/guide vs ~1–2 (prod 61/62). **"One does not…" mold: ZERO hits in all of 61–67 — the 359 deletion worked.** New watch: essay-opening deadpan family (4 members; max one in any sampler). Amichai persists in cmp-61 (baked into research_v2.md as predicted). Affective-landing near-miss baseline recorded per guide (65 closest; 67 none).
- **Task 3 — beta reader verdict: usable as a PROSE instrument; scores unusable for deltas.** 9 reads (61–67 + 65 repeat + cmp-62 via `--input-file`). Quote fidelity PASS (~30 verified, 2 word-level morphs); sag calls PASS (match independent close read; both 65 runs agree on v.6 template feel); caught real defects (dangling JPS footnote markers in Ps 64 text, unglossed "precative perfect"/"Ugaritic"). **Scores clump 6–9 (Emotional flat 7×8 reads) — discrimination FAIL; wit known-contrast FAIL** (pre-359 wit 7,6,7 vs post 6,6,7,7; decisive: cmp-62 with the author's favorite wit scored Wit 6 = prod-62). Landing-detector valid but threshold permissive (8/9 "found"; yet both 62 reads independently picked the same landing insight, and 67 — the one wit-my-read called "none" — got the only hedge + only EI 6). Noise: scores ±1 PASS; prose particulars noisy (65 runs contradict on Kafka vs Du Fu). Baseline table + 4 optional prompt-tightening edits in IMPLEMENTATION_LOG. Ps 66 method-summary stats artifact noted (0s/N-A).
- **Part 2 (same day, author decisions)**: **voice sampler WIRED** into `master_editor.py` RULE 13 — Ps 48 gold example deleted, replaced by 6 own-output Tier-1 exemplars w/ move annotations + anti-copy clause (net +621 chars/+1.3%; SI prompt inherits). **Live writer A/B on Ps 66** ($1.47, gitignored `_writer_compare_new/`, corrected wrapper for the archived `compare_writer.py` whose self-computed ROOT broke): **affective landing FIRED** (v.19 "the fear that the room was empty… says it simply" — but duplicated across intro+verses w/ recycled imagery; Stage 1/2 don't see each other — watch); beta-read cross-check picked the engineered landing verbatim w/ stronger intensity language (scores flat, as predicted — prose is the readout); human-mouth INCONCLUSIVE (no use-scene in 66's dossier); sampler gave fresh molds ("liturgy quoting its own libretto") but 2 phrase-level exemplar echoes → **swapped the prayer-topical "photograph" exemplar for Ps 64's shark/Macheath** (least parrot-prone). Verses −23% (echoes 10→5; thinned the flagged vv.11–15 pile-up but lost Rachel Bluwstein) — unattributable at n=1, watch next production psalm.
- **Part 3 (same day)**: head-to-head OLD (worktree @`6fd428f`) vs NEW pipeline on **Ps 68** (~$14.2; shared upstream ran once). Two DOCX in `Documents/Psalm study guide/` ("Psalm 68 (OLD/NEW pipeline).docx"). Results: **landing FIRED once, no duplication**, beta reader picked it verbatim ("once is enough to matter"); **zero sampler echoes** on a non-66 psalm; distributional facts visible in NEW sidecar (exact counts); length −9.5% (noise; 66's −23% didn't recur); **human-mouth UNDER-FIRED** (dropped the Huguenot-Poitiers scene OLD used; n=2 without a real scene — consider strengthening #11). Beta scores identical except EI where NEW scored LOWER (6 vs 7) despite the praised landing — score axis definitively dead for deltas. Worktree `../psalms_old_pipeline` retained for inspection.
- **Part 4 (session close)**: three approved lessons-to-edits. (1) **Landing dedup guard** — writer is ONE call, so 66's duplication was an instruction gap; added "one means one across the WHOLE guide… never rebuild it." (2) **Human-mouth → planning step** (#11 reworded): never fired in 2 tests + missed real material on 68; now "scan for the single best documented scene… a passing clause is a missed scene, not a told one; if none, skip." (3) **Beta reader**: machine-readable `LANDING: found/near-miss/none` line + Wit redefined as dry deadpan carrying the argument (warmth doesn't count). Writer prompt net for the whole session 48,082→49,075 (+2.1%); SI inherits. Session spend ~$16.5.
- **Next**: next production psalm (69?) runs the finalized pipeline — check the `LANDING:` line, whether human-mouth fires with the strengthened #11, exemplar echoes, length. Regenerate literary echoes for 61 (`run_literary_echoes.py 61`) to clear the Amichai poem. Remove the comparison worktree when done inspecting (`git worktree remove ../psalms_old_pipeline --force`).
**Session 363 (2026-07-05)**: Engagement/insight upgrades — affective landing + human-mouth scenes (writer), computed distributional facts (sidecar), beta-reader agent, wit-exemplar mining
- **Goal**: from a quality review ("more engaging, insightful, moving?"), the author approved 5 items; explicitly **rejected** cross-psalm insight tracking (readers often read one guide, not the collection — guides stay self-contained).
- **Writer prompt** (`master_editor.py`, net +1.5 KB / +3.2%): (1) **one affective landing per guide** — locate the psalm's emotional center of gravity and build ONE passage where analysis stops and the human point lands plainly (2-3 sentences, no device named, no source cited); RULE 7b still governs; checklist "zero is a miss; two is a dilution"; essay CLOSING may now be the affective landing. (2) items-of-interest **#11 rewritten → "The Psalm in a Human Mouth"**: a concrete scene of the psalm being *used* (who/when/what was at stake, words quoted at the moment of use) is first-rank material, ahead of one more lexical parallel. SI prompt inherits both (derives via `.replace`).
- **`src/concordance/distributional_facts.py`** (NEW, $0): deterministic SQL over tanakh.db per psalm — rare forms (≤5x Tanakh, all refs), repeated forms w/ verse lists, first/last-verse shared forms (computed inclusio candidates), rare adjacent pairs (≤3x), divine-name tallies — spliced into the Synthesis Scholar as `### COMPUTED DISTRIBUTIONAL FACTS` ("trust these counts over your memory"; header warns: exact FORMS, not lemmas). Wired in `discover_cross_verse_observations` (covers both pipelines); no-ops without a populated DB. Unit-tested vs synthetic DB; **not yet smoke-tested vs the real local DB** (`python -m src.concordance.distributional_facts 60`).
- **Beta Reader** (`src/agents/beta_reader.py` + `scripts/run_beta_reader.py`, NEW; STEP 5d in both pipelines, ON by default, `--skip-beta-reader`): simulated target-audience read of the finished guide → experience report (engagement curve, AHA/WIT/FELT moments quoted, affective-landing check incl. licensed "Nothing in this guide moved me," sag/template-feel, confusion, 5 parsed scores anchored at "competent but forgettable = 5-6," verdict). **Measurement-only**: no edits, feeds no revision pass. Sonnet 4.6, ~$0.08/psalm, non-fatal. Output `psalm_NNN_beta_read.md`.
- **`docs/prompts_reference/voice_exemplars.md`** (NEW): mined all reachable finals (Pss 58/59/60 prod + 3 A/B rounds; 61/62 exist only locally — add by hand). 10 Tier-1 wit exemplars mapped to RULE-13 moves ("…an instruction it spends the next eleven verses cheerfully ignoring"; "Evil here is meticulous. It keeps good books."; "A corpse instructs no one."), 6 Tier-2 texture passages, anti-patterns (the recurring "One does not…" template — exclude from any future sampler or the model re-anchors). **Not wired into prompts yet.**
- **Next**: execute `docs/plans/NEXT_SESSION_PROMPT_session_364_local_validation.md` (find real tanakh.db + smoke-test distributional facts; read last 7 finals + finish voice_exemplars with 61/62; run beta reader on recent outputs and meta-evaluate the agent, ~$1). Then: decide on wiring a voice sampler (replace RULE-13 text, don't grow); optional ~$1.3 writer-only A/B of the two prompt edits via archived `compare_writer.py`.
**Session 362 (2026-07-01)**: Sonnet 5 vs Sonnet 4.6 for the micro agent — investigated, live A/B on Ps 65, NOT adopted (all code reverted)
- **Question**: shift the micro analyst (only Sonnet user; 2 calls/psalm) from Sonnet 4.6 → Sonnet 5, "high or greater" thinking. **Pricing**: Sonnet 5 is $3/$15 standard but **$2/$10 intro through 2026-08-31**; new tokenizer nominally +30% tokens (but empirically **input was flat** on Ps 65's Hebrew-heavy prompt). **Migration breaks**: Stage 1's `budget_tokens` 400s on Sonnet 5 (must go adaptive+effort, no hard thinking cap); Stage 2's omitted `thinking` silently flips to adaptive-ON (must set `disabled`).
- **A/B (Ps 65, macro reused, Stages 1–2)**: `effort` is a coarse 3.3× lever — **`xhigh`** = 27.8K out, **−35%/−57%** cost, but **thinner** than 4.6 (34 vs 41 insights, half the note prose = the user's concern); **`max`** = 92.6K out, **+29%/+93%** cost, but **richer** (51 insights, 2× figurative, 15 questions). **Can't get cheaper AND richer at once** — 4.6 ($0.77) is the middle. **Quality (manual, vv.2/4/10)**: comparable; S5 sharper on cross-verse connective catches (תהלה/תפלה near-rhyme; גבר→גבורה echo v4→v7; peleg-Elohim superlative w/ Ps 80:11/36:7), 4.6 deeper per-item (more interpretive options, longer cross-ref chains, 510 vs 269 chars/insight). Better *scout*, weaker *essayist*.
- **Not adopted**: no clean win (cheap=thinner, rich=pricier-than-4.6). Full data + reproduction: `docs/plans/SONNET5_MICRO_AB_FINDINGS.md` (pointer in Reference Docs). All code restored from HEAD (`DEFAULT_MODEL` still `claude-sonnet-4-6`); throwaway runner removed.
**Session 361 (2026-06-28)**: Shimush Tehillim integration — systematic practical-Kabbalah coverage in liturgical section
- **Goal**: Shimush Tehillim (שימוש תהלים) content was occasionally surfaced by deep research and incorporated beautifully by the Master Writer, but never systematically. Made it a standard pipeline feature: deep research now explicitly targets it, Master Writer knows to create a `#### Practical Kabbalah` subsection (only when content exists), speculating on WHY the psalm was selected for that purpose.
- **Three edits, zero cost**: (1) `docs/prompts_reference/deep_research_prompt.md` +22 words naming Shimush Tehillim; (2) `src/agents/master_editor.py` Stage 2 +1 bullet (~55 words) with subsection guidance; (3) `data/deep_research/README.md` removed stale inline prompt copy, now points to canonical prompt file. No new agents, databases, or pipeline steps. Existing 55 deep research files left as-is — coverage appears incrementally on future runs.
**Session 360 (2026-06-23)**: Pipeline cost review → A1 (dossier prompt-cache) designed, blocker found, keepalive fix designed, SHELVED + documented
- **Review**: per-psalm cost ~$6 (Opus 4.8 ~$2.61 = Macro + Synthesis Discovery + Master Writer; GPT-5.4 ~$2.16; Sonnet ~$0.64; Gemini ~$0.41; GPT-5.1 ~$0.16). Produced a tiered menu (A1 dossier caching; A2 citation→Haiku; B1 no-op `ResearchTrimmer`/selective commentary; B2 copy-editor 5.4→5.1; C1 Synthesis Discovery skip/downsize). Key waste: Synthesis Discovery + Master Writer each re-ingest the SAME ~130–180k-token dossier with **zero prompt caching** (`cache_*_tokens: 0` everywhere); phonetic analyst is free (algorithmic); insights/questions already skipped by default.
- **A1 blocker + fix**: prefix caching is EV-negative naively — cache is written at Synthesis start, read when Writer starts, but Synthesis runs **303–466s > the 300s cache TTL**, so the cache expires first. Fix (user's idea): a **keepalive ping** (cheap `max_tokens=1` cache read) fired during Synthesis refreshes the TTL → reliable hit → **~$0.30–0.45/psalm** net.
- **SHELVED** over (a) the required Master-Writer prompt reorder (dossier must lead → risk to the tuned voice) and (b) model-swap fragility (caching only works if Synthesis+Writer share the SAME Anthropic model; a GPT writer kills it). Full plan: `docs/plans/DOSSIER_CACHE_KEEPALIVE_PLAN.md` (pointer added to Reference Docs). No code changed.
## Quick Commands

```bash
python scripts/run_enhanced_pipeline.py 23                             # Process single psalm (synthesis-discovery sidecar ON by default, ~+$2)
python scripts/run_enhanced_pipeline.py 23 --resume                     # Resume from last step
python scripts/run_enhanced_pipeline.py 23 --skip-synthesis-discovery   # Disable the Session-347 cross-verse synthesis sidecar
python scripts/run_si_pipeline.py 19                 # Special Instruction pipeline (synthesis-discovery sidecar ON by default)
python scripts/run_literary_echoes.py 53             # Standalone 4-pass literary echoes (default: regenerate)
python scripts/run_copy_editor.py 36 37 38           # Standalone copy editor
python scripts/run_beta_reader.py 58 59 60           # Standalone beta reader (reader-experience report, ~$0.08/psalm)
python scripts/run_scripture_verifier.py 41          # Standalone citation verifier
python scripts/converse_with_editor.py 21            # Chat with Master Editor
```

## Key Directories

- `src/agents/` — AI agent implementations (macro, micro, synthesis, editors, copy editor)
- `src/concordance/` — 4-layer Hebrew search system
- `database/` — SQLite databases (tanakh.db, psalm_relationships.db)
- `data/deep_research/` — Gemini Deep Research outputs
- `data/special_instructions/` — Author directive files for SI pipeline
- `output/psalm_*/` — Generated commentary (production)
- `scripts/` — Pipeline runners and utilities

## Reference Docs (read only when needed)

- `docs/session_tracking/scriptReferences.md` — All scripts with descriptions
- `docs/session_tracking/PROJECT_STATUS.md` — Pipeline phases, active features, databases
- `docs/session_tracking/IMPLEMENTATION_LOG.md` — Detailed session history (300+)
- `docs/session_tracking/FEATURE_ARCHIVE.md` — Detailed docs for completed features
- `docs/architecture/TECHNICAL_ARCHITECTURE_SUMMARY.md` — Full system architecture
- `docs/architecture/TOKEN_REDUCTION_PHASE_B.md` — Ready-to-implement token reduction tasks
- `docs/plans/DOSSIER_CACHE_KEEPALIVE_PLAN.md` — SHELVED cost-reduction plan (A1): prompt-cache the shared dossier across Synthesis Discovery → Master Writer via a keepalive ping (~$0.30–0.45/psalm); shelved over writer-reorder + model-swap fragility
- `docs/plans/SONNET5_MICRO_AB_FINDINGS.md` — SHELVED model swap: micro agent Sonnet 4.6 → Sonnet 5. Live A/B on Ps 65; `effort=xhigh` cheaper-but-thinner, `effort=max` richer-but-pricier-than-4.6; no clean win. Not adopted; all code reverted. Read before re-investigating a Sonnet 5 micro swap

## File Organization Rules

- **Production code:** `src/`, `main.py`, `scripts/`
- **Experimental/test:** Use during session, archive immediately after
- **Archive after 1 session:** Test scripts, debug outputs, temp files → `archive/`
- **Never commit:** `*.log`, `*_output.txt`, temp analysis files (in .gitignore)

## End-of-Session Checklist

1. **Update this file (CLAUDE.md)**: Increment session number (line 3), replace oldest of 5 recent entries
2. **Update IMPLEMENTATION_LOG.md**: Add detailed session entry at top
3. **Update scriptReferences.md**: If scripts were created or significantly changed
