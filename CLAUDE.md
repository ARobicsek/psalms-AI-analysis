# Psalms AI Commentary Pipeline

**Session**: 380 (2026-08-15)
**Phase**: Pipeline Production — tweaks and improvements

AI-powered system generating scholarly verse-by-verse commentary for all 150 Psalms using Claude (Opus 5, Opus 4.8, Sonnet 4.6), GPT (5.6 Terra, 5.1), and Gemini (3.1 Pro) with multi-agent pipeline and Hebrew concordance integration.

## Recent Work (Last 5 Sessions)
**Session 380 (2026-08-15)**: **THE RESEARCH BUNDLE HAS BEEN CUTTING 23% OF EVERY COMMENTARY ENTRY AT 400 CHARS** — found by reading the writer's own thinking; the superlative tic is THREE failures, not one, and the fix had to protect the concordance findings it looks exactly like
- **Trigger**: the author, reviewing the Ps 73 output. Two items: (1) *"I'm seeing multiple examples of an 'LLM verbal tic' — describing something as 'the most' something. Calderon's quote is 'the most famous soliloquy in Spanish'. (really?) … 'The second half is the most argued clause in the psalm' (how can you know that???) How might we approach this? in the writer? the copy editor?"*; (2) *"reading the thinking output there are a couple places where it mentions a commentary being cut off. what's happening there?"*
- **THE TRUNCATION WAS REAL AND FOUR SESSIONS OF WORK HAD WALKED PAST IT.** `research_assembler` capped every commentary quotation at **400 chars** and appended a bare `...` — two lines, unchanged for the life of the project. **1,119 of 4,907 entries corpus-wide (23%) are truncated; Ps 73 is 67 of 198 (34%).** Re-fetching all 67 from Sefaria (pure Python, **$0**) measured **19,273 characters discarded**, median 222, worst **2,395 — Malbim on 73:17, the sanctuary pivot, the psalm's structural hinge**. Romemot El, which the bundle's own header calls *"present on nearly every verse,"* is the most frequent casualty; Radak, *"expansive linguistic explanation,"* is second.
- **THE TOKENS WERE NEVER THE ARGUMENT.** Restoring everything grows the median bundle **2,877 chars** and the worst 19,273; measured across all 86 bundles, **NOT ONE crosses the writer's 350,000-char trim ceiling**, so nothing is traded away in the trimmer (`SynthesisWriter`'s tighter 300k ceiling is dead code — it is REMOVED from the pipeline). ~$0.05–0.12/psalm against an $8.56 run. **The real cost was that the writer READ the fragments and threw them away**: the S376 thinking capture has it rating Malbim v.15 and Torah Temimah v.17 **Tier 1** and then discarding both as *"cut off mid-thought."* We paid for the fetch, paid input tokens on the fragment, and paid thinking tokens on the writer working out that it was broken. **This is the first bug the thinking capture has found on its own** — S376 shipped it as a hypothesis generator and it earned its keep here.
- **Fix**: cap **400 → 2,000** (recovers 66 of Ps 73's 67 whole, still bounds a Torah Temimah entry quoting a whole sugya), marker `...` → **` […truncated]`** — a bare ellipsis is indistinguishable from one the commentator wrote, which is exactly why the writer had to *infer* the damage. **ONE definition, in `commentary_librarian` next to `CommentaryEntry`** (the dependency runs assembler → librarian); a test reads the module source to assert no second copy grew back. Also killed the librarian's own 300-char variant in its debug dump.
- **THE SUPERLATIVE TIC IS THREE DIFFERENT FAILURES WEARING ONE SURFACE FORM.** (a) *"the most famous soliloquy in Spanish"* — an appeal to external reputation the guide cannot source; (b) *"Leaven is the one thing barred from the altar"* — **FALSE, and refuted by the words quoted in its own sentence**, since the guide cites Lev 2:11 as *"no leaven AND NO HONEY"*; (c) *"the most argued clause in the psalm"* — a claim about scholarly consensus, unknowable in principle. Shared mechanism: **a superlative is a FREE INTENSIFIER** — it costs no evidence and adds rhetorical torque, so it gets reached for exactly where an argument needs force it has not earned. Case (b) is the tell: the superlative was doing the argumentative work (it pivots to *"a leavened heart may not come near"*), and the writer had the correct verse on screen while writing it.
- **THE HARD PART IS NOT CATCHING SUPERLATIVES, IT IS NOT CATCHING THE WRONG ONES.** 529 raw hits over 44 guides (1.62/1k words) — but hand-classifying **all 20 in Ps 73** gives **6 false positives** (*"the Most High"* renders עֶלְיוֹן; *"the greatest good"* is inside the Calderón translation), **8 VERIFIABLE concordance findings** (*"דּוֹר בָּנֶיךָ occurs nowhere else in the Bible"*, *"the poem's single Tetragrammaton"*) that are **among the best lines in the guide**, and **6 actual tics**. A blanket ban would destroy the concordance payoff. The discriminator is semantic, so it belongs to the copy editor, not a regex.
- **COPY EDITOR, NOT WRITER, ON FOUR GROUNDS**: all four instances **passed the copy editor untouched** (a gap, not a rule firing weakly); it is a local sentence-level failure, which is the copy editor's job description; the standing five-instance prior that additions to `master_editor.py` produce the opposite of restraint; and **`RULE 9` of the writer prompt says "Do not hedge"** — any "soften your superlatives" instruction collides with it head-on. The repair is **CUT OR GROUND, never HEDGE**, and the rule text says so.
- **THE DANGEROUS FALSE POSITIVE, CAUGHT BY MEASURING THE CORPUS BEFORE SHIPPING.** A first draft of the `fame` pattern matched `read`, which caught *"it is better read as"* and *"most read Job as parodying Ps 8"*. **`COPY_EDITOR_SYSTEM_PROMPT` lists "is best read as" TWICE as a sanctioned flagged conjecture the editor is forbidden to delete or weaken** — the audit would have set the copy editor against its own rule. Removed; a structural test blocks re-adding any interpretive verb. A too-broad `canonicity` pattern (`the most <any adj> <text-noun>`) was narrowed for catching *"the most honest line in the psalm"*, which is the author's aesthetic judgment about the text in front of him, not a claim about the world. **62 → 51 hits, and all 51 matched spans are genuine reception vocabulary.**
- **LIVE TEST ON PS 73, $0.69, isolated to `output/psalm_73/_superlative_test/` so the shipped run is untouched.** Same `print_ready.md` the original consumed, so it is a true A/B. **Five 9(h) fixes**: *"the most famous soliloquy in Spanish"* → *"a soliloquy"*; *"the most argued clause in the psalm"* → *"a debated clause"*; *"Leaven is the one thing barred"* → *"Leaven is barred"*; *"the Bible's most reserved divine attribute"* → *"a divine attribute the Bible guards closely"*; *"the best-documented moment"* → *"a well-documented moment"*. **The last two were never in my detector — the model generalised from the rule text.** The leaven note reads *"contradicted by the very verse it quoted, which also bars honey (Lev 2:11)"* — it reached the self-contradiction by reasoning, not pattern match. **All 7 protected concordance claims survived intact**; 13,590 → 13,510 words, so no collateral rewriting. **One genuine miss survived** (*"its most famous consolation"*).
- **Verified**: **156 tests pass** (133 + 23 new). Detector precision **51 reception hits across 33 of 89 delivered guides, zero false positives in the matched spans**.
- **Watch**: **no psalm has been GENERATED on the restored bundle** — every truncation figure is measured on bundles and on Sefaria, not on output, and the writer has never seen a full Malbim v.17. The 9(h) rule is validated on **one psalm, one arm, un-A/B'd against a beta reader**. The broad superlative family is deliberately **NOT gated** (~30% true-positive rate would cry wolf) — `--all` is review-only. **The copy editor has no access to the research bundle**: 9(h) tests uniqueness against quoted evidence and the model's own knowledge, which worked on the leaven case only because `RULE 8/8b` force the guide to show its evidence on the page; wiring the real bundle in via `supplementary_prompt` is ~270k chars against a ~29k-token input and was **not** done.

**Session 379 (2026-08-14)**: **THE ANALYTICAL FRAMEWORK IS OUT OF BOTH WRITER PROMPTS**; the SI pipeline's hand-copied splice had drifted TWICE and one drift was silently costing quality; the agenda's cleanup list named two sites that would have broken production
- **Trigger**: the author — *"please pick up the leftover work from the previous session"*, i.e. `docs/plans/NEXT_SESSION_PROMPT_session_378.md`, whose decision was already made on S378's two-dossier Ps 27 A/B.
- **THE BLOCK IS GONE. 75,777 → 75,697 chars, exactly the predicted −80.** `MASTER_WRITER_PROMPT_SI` is derived from V4 by a `.replace()` that never touches the INPUTS, so **one template edit covered both pipelines** (SI 76,202 → 76,122).
- **THE SI SPLICE ANCHOR — S378's named hard blocker — IS FIXED**, but fixing it by hand would have left two hand-maintained copies. Measuring the built prompts caught what that had already cost: the SI writer prompt came out **777 chars SHORTER** than the normal one despite carrying an extra directive section. The gap was the observations block — **the SI copy was still the pre-S371 text**, `(use where they fit; do NOT structure your commentary around them)` with blanket conjecture-hedging: **the exact wording S371 measured as suppressing the best idea in the Ps 71 dossier under Opus 5**. S371 rewrote it in `master_editor.py` and never mirrored it, while the SI comment claimed *"Mirrors the splice in MasterEditor."* **The comment had been false for eight sessions.** The splice is now ONE shared method, `MasterEditor._splice_cross_verse_observations()`, inherited by SI.
- **THE AGENDA'S CLEANUP LIST NAMED THE WRONG SITES — two of three would have broken things.** (a) `master_editor.py:912/951` are **not writer plumbing**: they sit in `run_synthesis_discovery()` and feed `SynthesisDiscoveryAgent`, **a different agent still using the framework**; arm F only ever cut the writer prompt string, so removing them is an un-A/B'd change to a $1.50/psalm agent. **Left in place.** (b) The `analytical_framework: str` **parameter cannot be removed**: `write_commentary` delegates to `MasterEditorV2` (in `archive/`), which passes it **by keyword** — dropping it raises `TypeError` on every production run. Both overrides keep it, commented `UNUSED`. Only the `.format()` kwarg and SI's own RAG load were genuinely dead. **`str.format()` ignores extra kwargs, so none of this would have raised** — it was traced, not pattern-matched.
- **THE LEGACY-BUNDLE TRAP IS CLOSED AT THE LOAD PATH.** 54 bundle files still carry the inline `## Analytical Framework for Biblical Poetry` section (34 canonical dossiers, psalms 1–34, plus trimmed copies and legacy dirs), uniformly **25,794 chars — 2.6× the block just removed**; a writer-only re-run feeds it straight back through the dossier. New `ResearchTrimmer.strip_analytical_framework()` had to be called **before `trim_bundle`'s early size return**, which fires on every real bundle (~226k against a 350k ceiling) — anything after it would never run. Validated across all 86 bundles: 34 stripped with every heading preserved in order, **52 modern bundles byte-identical**.
- **Verified**: **133 tests pass** (121 + 12 new). SI is now exactly V4 + `SI_SECTION` (asserted). **The check that would have caught the S378 confound**: built the real writer prompt for both pipelines on Ps 27's live 17,318-char synthesis-discovery file, model call stubbed, **$0** — both splice against the READER QUESTIONS anchor, no unrendered placeholders, no-observations control byte-identical. `--arms F_no_framework` now **raises in pre-flight**, the intended end state. The `.md` file is **still on disk and still loading** (9,239 chars) for macro/micro/research_assembler.
- **Honest framing**: the A/B showed removal **costs** nothing; it did not show removal **gains** anything. Shipped for hygiene. **No psalm has been generated on the new prompt** — every delta above is measured on built prompts, not output.
- **Watch**: **running `pytest` at the repo root EXECUTES stray debug scripts** — one made a live OpenAI call and rewrote tracked `test_api_out.txt` (reverted); the 5 root "errors" are this, not test failures. **Use `pytest tests`.** The SI pipeline now gets the S371 observations guidance **for the first time** — production-validated on the normal pipeline, never run through SI. Still open from the S378 list: the **LXX budget is not biting** (Ps 27 at 64%), the **thinking capture collides across A/B arms**, **prompt caching for A/Bs** (worth building at N≥3, a loss at 2).

**Session 378 (2026-08-14)**: **THE ANALYTICAL FRAMEWORK EARNS NOTHING — measured twice on Ps 27 and cleared for removal**; a pipeline shipped a DOCX whose entire commentary was the copy editor saying "I don't see the commentary"; the splice anchor was a one-variable trap
- **Trigger**: the author — *"when we built this pipeline months ago I created a section that was to be included in every research bundle… a dense primer on reading biblical poetry… is that still in there? does it need to be?"* Then: run the A/B on Ps 27, *"minimum API re-run… just the writer"*, then *"what should be running is everything that would run from: python scripts/run_enhanced_pipeline.py 27"*.
- **THE AUTHOR'S MEMORY WAS RIGHT AND SO WAS HIS DOUBT.** `docs/architecture/analytical_framework_for_RAG.md` **was** a `## Analytical Framework for Biblical Poetry` section in every research bundle — S66 (2025-11-02) put it there in a 27,678-char prose version; **56 bundles on disk still carry it** (psalms 1–34 + legacy test dirs). S256 rewrote it telegraphically (−61%); **S257 deleted the bundle copy as a *"duplication bug"* — the writer had been reading the whole thing TWICE**. What survives is the `{analytical_framework}` block in the writer's INPUTS. Both prior cuts were **cost** decisions; nobody had ever asked whether the writer should have it at all.
- **A/B'd TWICE ON PS 27, ON TWO DIFFERENT DOSSIERS, NO CAPABILITY DIFFERENCE EITHER TIME.** Thin (Jan-2026) dossier: words 7,562 → 7,547, device vocabulary 18 → 16, citations 29 → 30, coverage 14/14 both. Full fresh pipeline: words 8,838 → 8,014, **device vocabulary 5 → 5**, citations 23 → 21, coverage 14/14 both. The framework is **4,601 tok ≈ $0.023/psalm** — cost is not the argument.
- **THE EVIDENCE IS CONVERGENCE, NOT COUNTS.** On the full dossier both arms independently found the six hapax `־נִי` verbs, the סתר reversal (hide *me* = salvation / hide *Your face* = ruin), **Psalm 31 as the control case** proving the split deliberate, the twice-only `אֲבַקֵּשׁ` equating "the one thing" with "the face", the Rabia al-Adawiyya echo, and the Proverbs `יָפִיחַ` perjury formula. Removing the primer removed none of them. Arm F's own thinking capture — **no poetics reference in its prompt** — used *parallel* ×6, *inclusio*, *wordplay* ×2, *metaphor* ×3, and its v.1 turned on the `אוֹרִי`/`אִירָא` near-anagram, i.e. the framework's own §III.1 Paronomasia. **Stated honestly: the A/B shows removal COSTS nothing; it does not show removal GAINS anything.** Ship for hygiene — no rule invokes it, the only other uses of "framework" in the prompt are a warning to keep it out of the output and an entry in BLURRY WORDS TO WATCH; it teaches Lowth's taxonomy under an Alter banner; 33 dangling `[Ref##]` markers.
- **THE PIPELINE SHIPPED A GUIDE WHOSE COMMENTARY WAS THE COPY EDITOR'S REFUSAL.** `run_enhanced_pipeline.py 27` exited **0** having written a 41,790-byte DOCX whose intro and verses both read *"I don't see the Psalm 27 commentary text in your message. Please paste the full commentary…"*. The writer was fine (51,486 chars); **`_parse_writer_response`'s `###?` matches only `##`/`###`** and this run emitted `# INTRODUCTION ESSAY` / `# VERSE COMMENTARY` as **H1** — so both sections parsed empty, **which also disabled the S374 fallback** (it requires `verse_match`), and the copy editor ran on nothing. Heading level is the model's to choose and varies run to run on identical input. Fixed to `#{1,4}` in all three patterns; the real commentary was recovered from the saved response at **$0**, so the $1.9454 writer call was not lost.
- **THE SPLICE ANCHOR WAS A TWO-VARIABLE TRAP, CAUGHT ONE MINUTE INTO A PAID RUN.** `### ANALYTICAL FRAMEWORK …` doubles as the anchor for the S347 cross-verse observations block, and a missing anchor only **logged a warning**. On the fresh dossier (which has a synthesis-discovery file, unlike the Jan-2026 one) arm F would have lost the framework **and** the entire 17,318-char observations block — **~$1.50/psalm** — while looking like a one-variable arm. `master_editor.py` now uses a **fallback chain** (framework header → `### READER QUESTIONS`), which puts the block in the same position in both variants and leaves framework-carrying prompts byte-identical. **`master_editor_si.py:247` still has the old single anchor — the SI pipeline will silently lose observations if the header is removed before that is fixed.**
- **A completed pipeline run's writer pass IS the base arm.** Verified the pipeline and `ab_writer_prompts.py` feed the writer identically (`insights_file=None` both; `reader_questions_file=None` both when no such file exists; same synthesis-discovery file; same template), so base never needs paying for twice. New **`scripts/ab_seed_base_from_pipeline.py`** recovers it from the saved response — **$1.95 saved**, and it refuses to seed when the comparison would be invalid. Its own self-test caught the trap it now guards: `output/debug/master_writer_v4_response_psalm_N.txt` is a **single slot overwritten by every writer call**, so seeding *after* an arm runs fills `base` with that arm's text — an A/B whose two arms are the same arm.
- **CACHING WAS EVALUATED AND DECLINED, NOT BUILT.** The arms share a ~95% prefix and diverge *after* the research bundle, so **no reordering is needed** (unlike the shelved dossier-cache plan, which was about a shared *middle*). But a writer call runs ~10–12 min against a **5-minute** default TTL, and the 1-hour TTL costs **2× on write**: at two arms that is 2.0× + 0.1× vs 2.0× uncached — a **loss**. At five arms, 2.4× vs 5.0×. Worth building at N≥3; documented, not implemented.
- **Deliverables**: `Documents/Psalm study guide/Psalm 27 (Baseline).docx` (68,771 bytes) and `Psalm 27 (no analytical framework).docx` (65,565 bytes), both with full modern methodology blocks. Session spend **~$14.40** (thin-dossier A/B $4.50 · full pipeline $6.6231 · arm F $1.9594 · finishing $1.3152). **The experimental record is in `archive/psalm_27_S378_abrun2_full_pipeline/`** (both arms' raw writer output, both prompt templates, the shared synthesis-discovery block, arm F's thinking capture, and a README) — `output/` is gitignored, so nothing from either A/B would otherwise survive in the repo.
- **Watch**: `master_editor_si.py` anchor is the **one hard blocker** for adoption; **do NOT delete the .md file** — `RAGManager._verify_files()` raises and `get_rag_context()` loads it unconditionally for macro, micro **and** research_assembler; the **56 legacy bundles** feed the old 27,730-char copy back in on any writer-only re-run; the **LXX budget is not biting** (`check_lxx_density.py 27` = **64%, OVER by 4 verses** against RULE 8b's ≤40%, first fresh psalm measured since S375 set it); the **thinking capture collides across A/B arms** and lost the base arm's reasoning in both runs. Full plan: `docs/plans/NEXT_SESSION_PROMPT_session_378.md`.

**Session 377 (2026-08-07)**: **COST AUDIT OF THE PS 73 RUN — the $8.5571 total was right and five pricing rows were still wrong**; a THIRD duplicate rate table; the dossier-cache plan re-shelved on measurement; thinking captures moved into the psalm's folder
- **Trigger**: the author, on the Ps 73 cost summary — *"1. did we get all the pricing right based on current prices? 2. are you seeing any obvious inefficiencies such as an opportunity to use a less costly model?"* Then: *"shelve the plan to cache; document reasons. also please move where we store the model thinking to the psalm's output folder."*
- **EVERY RATE THE RUN TOUCHED WAS CORRECT** — re-pricing the saved `psalm_073_cost.json` under the corrected table returns **$8.5571 to four decimals**, so no historical figure moves. All five defects are **latent**: they misprice paths that exist but were not exercised, which is the hardest kind to notice.
- **`cache_read` was 0.0 on FOUR rows** under a "Not applicable" comment — gpt-5/5.1 ($0.125), gpt-5.4 ($0.25), gemini-3.1-pro ($0.20) all bill a cache hit at 10% of input, so a cached token would have priced at **zero** the moment a caller wired it up. The new `test_cached_input_is_never_free` asserts `cache_read == 0.10 × input` on every row and **immediately caught a fifth** (legacy `gpt-5`) the manual pass had missed.
- **OpenAI has added LONG-CONTEXT TIERS to two models we use**: gpt-5.4 $5.00/$22.50, terra $4.00/$18.00. Recorded as **caveat comments, not code** — `CostTracker` accumulates per-model *totals* and has no per-call prompt length, so a tier cannot be applied without pricing at the call site. Did not bite Ps 73: the tier is per **request**, and Terra's 395k input tokens arrive as ~26 small calls, never one oversized prompt.
- **`claude-sonnet-5` and `claude-fable-5` had NO ROWS, and the fallback was silent zeros** — the shelved Sonnet-5 micro A/B would have scored its cost arm at **$0.00** with nothing said. Both rows added; `resolve_pricing()` now returns `None`, `CostTracker` warns and stamps **`*** FLOOR, NOT ACTUAL ***`** on the grand total plus an `unpriced_models` key (emitted only when non-empty, so existing cost JSONs keep their shape), and `price_tokens()` raises. The split is deliberate: the tracker must not destroy the report of a run already paid for; a single-call helper has no reason to return a wrong number.
- **Sonnet 5's intro pricing EXPIRES ON ITS OWN** — the row holds the durable $3/$15 and a separate `INTRO_PRICING` override supplies $2/$10 through 2026-08-31. Encoding the promo *in* the row with a comment saying it changes later is exactly how *"cost-neutral by construction"* survived six weeks in S373.
- **A THIRD DUPLICATE PRICING TABLE, in `figurative_curator.py`**: `GPT54_INPUT_COST_PER_M = 2.50`/`15.00` while `MODEL` had been `gpt-5.6-terra` ($2.00/$12.00) since S367 — inflating the curator's own reported cost **25%** ($0.3700 → $0.2960 verified). Run totals were never affected. S373 found this in `cost_tracker`, S374 in `literary_echoes_agent`; constants deleted, curator now calls the shared `price_tokens()`, and a test greps the module so a fourth copy cannot be added.
- **Where the $8.56 went** (reconstructed from `DEFAULT_MODEL` + call counts — there is **no per-agent attribution** in the cost JSON): writer $2.4972 · macro+SD $1.8867 · echoes $1.3996 · micro $1.0962 · copy editor $0.8195 · figurative+question curators $0.5146 · librarian $0.3433. **output+thinking is 60%** of spend, input 40%.
- **A GEMINI GENERATION RETURNED NOTHING AND COST $0.16** (1.9% of the run): 3 tracked Gemini calls vs 2 recorded passes; the ghost is 6,785 input (identical to pass_1a's), **0 output**, 12,254 thinking. That is S374's empty-generation retry firing. The retry is right; the tax is real. It was findable only because the echoes report is honest on **cost** (reads the ledger, counts retries) and not on **tokens** (counts only calls that returned).
- **Echoes Pass 3 caches only 19% of its prompt** — 20 calls, 350,855 prompt tokens, 65,925 hits. OpenAI auto-caches prefixes ≥1024 tokens, so a shared preamble should hit far higher. Worth **~$0.35/psalm at no quality risk**. **Inferred from the hit rate, NOT from reading `_build_pass_3_prompt`; NOT DONE.**
- **THE DOSSIER-CACHE PLAN'S OWN PREDICTED FAILURE HAS OCCURRED.** It warned the win was *"coupled to both Opus calls running on the same Anthropic model"*; **S373 split them** (writer Opus 5, SD Opus 4.8) and Anthropic caches are model-scoped, so it **cannot work today** — its guard rail evaluates false on every run. Measured, not estimated: writer prompt 437,851 chars, SD 362,947, **common leading prefix 10 chars**, longest shared block **315,731 chars** (72% of the writer prompt) at offsets 67,478 / 17,325. The dossier is a shared **middle**; caching is prefix-only. **A $0.30–0.65/psalm estimate given to the author mid-session was withdrawn and corrected on this evidence.** Also: the writer is **one call**, so caching it alone is a pure 1.25× write with no read — **+$0.31/psalm, a loss**. The real prize is `ab_writer_prompts.py` (N arms, one dossier, one model by construction): **$6.19 → ~$2.98 per 5-arm A/B**. Catch: the reorder is itself a prompt change, so you need an A/B to justify the change that makes A/Bs cheap. **Author's call: shelve.**
- **Thinking captures moved to the psalm's folder** via new `src/utils/debug_paths.py` — `output/psalm_NN/psalm_0NN_{agent}_thinking.txt`. The helper exists because two naming facts would otherwise be re-derived at each call site: the **directory is unpadded** while the **files inside are zero-padded**, and **both directory forms exist on disk** (an existing one always wins, so an older psalm does not grow a second folder). Writer + copy editor; the copy editor's raw *response* stays in `output/debug`. **Forward-only.**
- **Verified**: 121 tests pass (7 new path tests, 9 new pricing tests); Ps 73 re-prices identically; Ps 73's writer thinking file exists at 23,706 chars — **S376's capture fired on its first production psalm** and its warning branch stayed quiet.
- **Watch**: the Pass-3 reorder is identified and **not done**; long-context tiers are **documented, not enforced**; `INTRO_PRICING` has never crossed its expiry in production (the test proves the mechanism, not the calendar); **no live run has exercised the new thinking path** — same shape as S376's own caveat.

**Session 376 (2026-08-06)**: **THE WRITER'S REASONING HAS BEEN GENERATED, PAID FOR, AND DISCARDED SINCE THE OPUS 4.7 MIGRATION** — one parameter turns it back on, at $0
- **Trigger**: the author, on the guides still leaving things unexplained (*"especially Talmudic ideas"*) and on whether a 70,000-character prompt is self-defeating: *"is there a way to actually watch the inner workings of the writer model as it goes about its thinking and writing… this might allow us to see if it's getting tripped up in knots. This may not be possible."*
- **IT IS POSSIBLE, AND THE PLUMBING WAS ALREADY THERE.** `_call_claude_writer` has always sent `thinking={"type": "adaptive"}` and has caught `thinking_delta` events since Session 327 — but kept only `len()` of them, and **never set `display`**. On Opus 4.7+ the default is `"omitted"`, which streams thinking blocks whose text is the **empty string**. So Session 327's instrument was measuring a quantity that had become structurally zero.
- **IT WORKED WHEN IT WAS WRITTEN AND DIED WITHOUT A SOUND.** Session 327 was built against Opus 4.6, whose default *was* `"summarized"`. Opus 4.7 flipped the default and introduced `display`; production cut over to Opus 5 in Session 373. Nothing errored. The only symptom was a log line that stopped printing — **the same silent-loss shape as literary-echoes Pass 4 (S374) and the copy editor's quotation marks (S373)**, and the third instance of an instrument going dark across a model migration.
- **Verified live, both halves, before shipping** (2 calls, ~$0.015): `display="summarized"` → **560 chars of thinking**; the production default → **0 chars**. The bug is not inferred from documentation, it is reproduced.
- **Fix**: `thinking.display = "summarized"` gated by `THINKING_DISPLAY_MODELS`, and the text accumulated and written to `output/debug/{prefix}_thinking_psalm_N.txt` beside the response. **Costs nothing** — `display` controls visibility only; the thinking happens and bills into `output_tokens` either way. Gate excludes Opus 4.6 / Sonnet 4.6, which already default to `"summarized"` and do **not** accept the parameter (same shape as `apply_effort` omitting `output_config` for old models); substring matching verified against nine IDs — `opus-4-5` does not false-match `opus-5`.
- **The instrument now reports its own death**: requesting summarized thinking and receiving none logs a WARNING naming the model. Session 327's failure was that an empty count is indistinguishable from a quiet run.
- **What it can and cannot answer, stated plainly.** It returns a model-generated **summary**, never the raw chain of thought, and can rationalise after the fact — a hypothesis generator, confirmed with `ab_writer_prompts.py`, not proof. Activations and attention over the prompt are **not available on Opus 5 at all** and no proxy was substituted for them. On the Talmudic item it does separate the two diagnoses that call for opposite fixes: the writer weighing the inferential step and cutting it (something competes with RULE 5 — subtract) versus it never arising (RULE 5 is not reaching the behaviour — a demonstration, not more rule text).
- **The author's "70K chars is getting in our own way" hypothesis is half right, and the project's own record says which half.** Length is not the variable — **arm B WON at +7,325 chars** and arm A's deletion made the guide *longer*. The five-instance standing prior is about a **kind** of instruction (exhortation), not a character count. So the experiment is a conflict audit, not a shortening pass; **NOT RUN THIS SESSION.**
- **Also committed**: an uncommitted intro-recovery fix already in the working tree from a prior session — `_parse_writer_response` now falls back to "everything before VERSE COMMENTARY" when the writer omits the `### INTRODUCTION ESSAY` header, which on Ps 1 had produced a **zero-byte introduction** that the copy editor then ran on. Not this session's work; **untested by this session.**
- **Watch**: **no writer run has yet exercised the capture** — the author will read the first thinking file on his next psalm, and the `elif "display" in thinking_cfg` warning branch is unexercised live. The `RULE 5` / Talmudic question is **diagnosed only when that file is read**. The prompt conflict audit is the open agenda item.

## Quick Commands

```bash
python scripts/run_enhanced_pipeline.py 23                             # Process single psalm (synthesis-discovery sidecar ON by default, ~+$2)
python scripts/run_enhanced_pipeline.py 23 --resume                     # Resume from last step
python scripts/run_enhanced_pipeline.py 23 --skip-synthesis-discovery   # Disable the Session-347 cross-verse synthesis sidecar
python scripts/run_si_pipeline.py 19                 # Special Instruction pipeline (synthesis-discovery sidecar ON by default)
python scripts/run_literary_echoes.py 53             # Standalone literary echoes (default: regenerate). Session 374: Pass 4 removed, Pass 3 verifies per entry, rebuild is deterministic
python scripts/run_copy_editor.py 36 37 38           # Standalone copy editor
python scripts/run_beta_reader.py 58 59 60           # Standalone beta reader (reader-experience report, ~$0.08/psalm)
python scripts/run_scripture_verifier.py 41          # Standalone citation verifier
python scripts/converse_with_editor.py 21            # Chat with Master Editor
python scripts/run_docx_only.py 1                    # Rerender the DOCX from existing markdown ($0)
python scripts/check_lxx_density.py 1                # Session 375: score against RULE 8b's LXX budget (<= 2 verses in 5); --all
python scripts/check_superlatives.py 73              # Session 380: audit ungrounded ranking claims. DEFAULT = reception claims only (gates, exit 1); --all adds the broad family for REVIEW (not errors); --delivered
python scripts/lineate_poem_translations.py 1        # Session 375: BACKFILL poem-translation line breaks from the echoes dossier; --dry-run
python scripts/ab_seed_base_from_pipeline.py 27      # Session 378: seed a prompt-A/B's `base` arm from a finished pipeline run ($0, ~$2 saved). RUN BEFORE ANY ARM
```

**Writer-prompt A/B, cheapest correct order** (Session 378 — never pay for `base` twice):
```bash
python scripts/run_enhanced_pipeline.py 27                      # full dossier + the production writer pass
python scripts/ab_seed_base_from_pipeline.py 27                 # that pass IS arm `base`; $0. MUST run before any arm
python scripts/ab_writer_prompts.py 27 --arms <arm>             # only the arms that actually vary, ~$2 each
python scripts/ab_finish_arms.py 27 --ab-dir _prompt_ab --arms base <arm> \
    --writer-model claude-opus-5 --copy-to-documents            # copy edit + DOCX, ~$0.65/arm
```

## Key Directories

- `src/agents/` — AI agent implementations (macro, micro, synthesis, editors, copy editor)
- `src/concordance/` — 4-layer Hebrew search system
- `database/` — SQLite databases (tanakh.db, psalm_relationships.db)
- `data/deep_research/` — Gemini Deep Research outputs
- `data/special_instructions/` — Author directive files for SI pipeline
- `output/psalm_*/` — Generated commentary (production)
- `scripts/` — Pipeline runners and utilities

## Reference Docs (read only when needed)

- `docs/session_tracking/scriptReferences.md` — All scripts with descriptions
- `docs/session_tracking/PROJECT_STATUS.md` — Pipeline phases, active features, databases
- `docs/session_tracking/IMPLEMENTATION_LOG.md` — Detailed session history (300+)
- `docs/session_tracking/FEATURE_ARCHIVE.md` — Detailed docs for completed features
- `docs/architecture/TECHNICAL_ARCHITECTURE_SUMMARY.md` — Full system architecture
- `docs/architecture/TOKEN_REDUCTION_PHASE_B.md` — Ready-to-implement token reduction tasks
- `docs/plans/DOSSIER_CACHE_KEEPALIVE_PLAN.md` — SHELVED cost-reduction plan (A1): prompt-cache the shared dossier across Synthesis Discovery → Master Writer via a keepalive ping (~$0.30–0.45/psalm); shelved over writer-reorder + model-swap fragility
- **PROMPT STATE (Session 375)**: `master_editor.py` IS arm E — verified byte-identical to `output/psalm_71/_prompt_ab/E_translation/_prompt_template.txt` before shipping, then given four author-requested, **un-A/B'd** deltas: Metzudat David removed (−31), RULE 5 rewritten to demand the inferential STEP (+2,755), RULE 12's `HOW TO SET A QUOTED POEM` format spec, and RULE 8b's **Septuagint budget** (≤ 2 verses in 5) plus its pointer at angles item 7. **75,777 chars.** Build any new arm as a delta from production — `variant_b`/`variant_d`/`variant_e` in `writer_prompt_variants.py` are no-ops that RAISE during pre-flight, which is intended. Known dangling reference, left deliberately: RULE 8/8b still cite `PHRASE COVERAGE`, which arm E deleted. **RULE 3b-2 and RULE 5 are byte-identical to Session 373** — Session 375's additions to both were built and then reverted at the author's request. **Session 379: the `{analytical_framework}` INPUT block is REMOVED — the prompt is now 75,697 chars** (−80), A/B'd twice on Ps 27 with no capability difference and shipped for hygiene, not for measured gain. **No psalm has been generated on it yet.**
- **THE CROSS-VERSE SPLICE IS ONE SHARED METHOD (Session 379)**: `MasterEditor._splice_cross_verse_observations()` puts the Session-347 observations block into the writer prompt, and `MasterEditorSI` **inherits** it. It used to be hand-copied into both files, and both drifts that caused are the lesson: the SI copy kept the **old single anchor** (a missing anchor only logs a warning, so removing the framework header would have silently dropped ~$1.50/psalm of synthesis discovery) **and** it still carried the **pre-Session-371 guidance** — the `do NOT structure your commentary around them` wording that S371 measured as suppressing the best idea in the Ps 71 dossier — while its comment claimed to mirror MasterEditor. **Never re-duplicate this method.** The anchor is a fallback chain (framework header → `### READER QUESTIONS (initial questions)`); since S379 removed the framework header, READER QUESTIONS is the one that fires. `MASTER_WRITER_PROMPT_SI` is derived from `MASTER_WRITER_PROMPT_V4` by a `.replace()` that inserts the SI section before `## YOUR INPUTS`, so **one template edit covers both pipelines** — a test asserts `len(SI) == len(V4) + len(SI_SECTION)`
- **THE FRAMEWORK IS GONE FROM THE PROMPT BUT LIVE EVERYWHERE ELSE (Session 379)**: **do not delete `docs/architecture/analytical_framework_for_RAG.md`** — `RAGManager._verify_files()` raises when it is missing and `get_rag_context()` loads it unconditionally for macro, micro and research_assembler. **`SynthesisDiscoveryAgent` genuinely still uses it** (`master_editor.py` ~912/~951, in `run_synthesis_discovery`, NOT writer plumbing — the Ps 27 A/B never tested removing it there). The `analytical_framework` parameter on both `_perform_writer_synthesis` overrides is **UNUSED but cannot be deleted**: the inherited `MasterEditorV2.write_commentary` passes it by keyword and would `TypeError`. Legacy bundles carrying the old inline `## Analytical Framework for Biblical Poetry` prose section (54 files, ~25,794 chars each) are stripped at load time by `ResearchTrimmer.strip_analytical_framework()`, which must stay **before** `trim_bundle`'s early size return or it never runs
- **COMMENTARY ENTRIES ARE CAPPED AT 2,000 CHARS, IN ONE PLACE (Session 380)**: `commentary_librarian.truncate_commentary` / `COMMENTARY_ENTRY_MAX_CHARS`, imported by `research_assembler` and **never redefined** — a test reads the module source to prove there is no second copy, because a duplicated constant has now cost this project three sessions. The cap was **400** and silently discarded **23% of every commentary entry in the corpus (1,119 of 4,907)**; on Ps 73 that measured **19,273 chars across 67 entries**, worst 2,395 on **Malbim 73:17, the psalm's structural hinge**. Restoring it grows the median bundle 2,877 chars and **no bundle crosses the 350k trim ceiling**. Two rules if you touch this: (1) **never re-slice `comm.hebrew`/`comm.english` at a call site** — a test greps for it, and the librarian's debug dump had its own 300-char copy of the same bug; (2) **the truncation marker must stay explicit** (` […truncated]`) — the old bare `...` read as the commentator's own ellipsis, so the writer had to *infer* the damage and silently dropped two Tier-1 commentaries rather than report a problem
- **UNGROUNDED SUPERLATIVES ARE COPY-EDITOR CATEGORY 9(h) (Session 380)**: patterns in `src/utils/superlatives.py`, spliced into `COPY_EDITOR_SYSTEM_PROMPT` by the same fail-at-import anchor discipline as `banned_phrases`. **This is NOT a banned-phrase list and must never become one** — only ~30% of raw superlative hits are the tic; 8 of Ps 73's 20 were **verifiable concordance findings** (*"occurs nowhere else in the Bible"*) that are among the best lines in the guide, and the rule text has an explicit **LEAVE ALONE** clause protecting them. Two traps, both already sprung once: (1) **do NOT re-add `read` to the `fame` pattern** — it matches *"it is better read as"*, which `COPY_EDITOR_SYSTEM_PROMPT` sanctions **twice** as a flagged conjecture the editor must not weaken, and a structural test now blocks any interpretive verb; (2) **do NOT gate on the broad candidate tier** — `check_superlatives.py --all` is review-only, and warning at a ~70% false-positive rate would train the author to ignore the channel. The repair is always **cut or ground, never hedge** (`RULE 9` of the writer prompt forbids hedging)
- **WRITER THINKING CAPTURE (Session 376; path changed Session 377)**: the Master Writer's reasoning is written to **`output/psalm_N/psalm_00N_master_writer_v4_thinking.txt`** on every Claude writer run, at **$0**. Session 377 moved it out of the flat `output/debug/` dump into the psalm's own folder via `src/utils/debug_paths.py` — use `thinking_file(psalm_number, agent_prefix)` rather than building the path by hand, because the **directory is unpadded** (`psalm_73`) while the **files inside it are zero-padded** (`psalm_073_*`), and both directory forms exist on disk. **Forward-only**: captures written before Session 377 are still under `output/debug/` — `thinking.display` controls visibility only, and the tokens are billed into `output_tokens` whether or not you ask to see them. Two rules if you touch `_call_claude_writer`: (1) **`display` must be gated by `THINKING_DISPLAY_MODELS`** — pre-4.7 models default to `"summarized"` and do not accept the parameter, so passing it unconditionally would 400 any Opus 4.6 A/B arm; (2) **never reduce the thinking to a count again** — that is exactly what let the Session-327 instrument die unnoticed across the 4.7 migration. What comes back is a **summary**, not the raw chain of thought (never available on Opus 5), so read it as evidence about what the writer weighed and confirm with `ab_writer_prompts.py`
- **QUOTED POEMS (Session 375)**: `document_generator.py` owns the three-part shape — original (italic) / 6pt seam / translation (**upright**) / attribution — in `_classify_quote_lines` + `_render_quote_block`. A bare `>` inside a quote block is a stanza separator and must not be dropped; ` / ` inside quoted verse is a LINE BREAK, not a slash (guard: never on a line containing `**`, which marks a chiasm diagram). Changing any of this: re-run the old-vs-new regression over all finished guides (7,540 paragraphs, 16 psalms expected to differ)
- **LITERARY ECHOES (Session 374)**: `src/agents/literary_echoes_parser.py` owns all parsing, merging, and reconstruction — pure functions, 41 unit tests, no SDK imports. **There is no Pass 4**; the final document is assembled in Python, which is why it can no longer truncate. Read the module docstring in `literary_echoes_agent.py` before changing anything there: it carries the measurement tables for the truncation bug and for the nine second-generator configurations that do not work. **Psalms 69-72 still hold dossiers truncated by the old Pass 4** and would need re-running to recover those echoes
- `docs/plans/NEXT_SESSION_PROMPT_session_378.md` — **CONSUMED (Session 379)**: the framework removal, the SI splice-anchor blocker, and the legacy-bundle trap are all done. Keep it for the two-dossier A/B evidence and for the record that **its stated edit sites were wrong** — two of the four "dead plumbing" sites belong to `SynthesisDiscoveryAgent` or to the inherited V2 caller and would have broken production (see Session 379 above). **Still open**, its three non-blocking finds: the LXX budget is not biting (Ps 27 at 64%), the thinking capture collides across A/B arms, and prompt caching for A/Bs (worth building at N≥3 arms, a loss at 2)
- `docs/plans/NEXT_SESSION_PROMPT_session_372.md` — the standing agenda behind it (unchanged by Session 373, which handled the author's post-read items instead): (a) the AFFECT work — pathos / wit / **wonder**, where "wonder" means the **POET's** powerful emotion, not the reader's AHA (author's explicit correction; read the definition table before touching anything); (b) score B4, whose scorecard was never read; (c) an Opus 5 vs 4.8 A/B with all the "B" changes, and whether the answer is a split-model run. All three set by the author at the close of Session 371
- `docs/plans/SESSION_372_TRANSLATION_SLOT_AND_JUDGE_VARIANCE.md` — **READ BEFORE TRUSTING ANY BETA-READER NUMBER, AND BEFORE ANY WRITER-PROMPT WORK**. The judge-variance calibration (4 reads of one fixed text) that invalidates most Session 370-371 affect/citation reasoning and INVERTS Session 371's arm-A grammar finding; the author's translation-slot design that dropped commentator mentions to 32, the lowest ever; and why `300-500 words per verse` was the length driver all along
- `docs/plans/SESSION_371_AB_RESULTS.md` — **READ BEFORE ANY FURTHER WRITER-PROMPT WORK**; note Session 372 corrected its arm-A `UNEXPLAINED GRAMMAR` claim (see above): the six-arm Opus-5 A/B/C on Ps 71. Positive exemplars WON while making the prompt 7,325 chars *longer*; the conciseness instruction BACKFIRED; deleting the verification scaffolding is a trade (+13% length, but no capability regression and wit 3→6). Also: the coverage↔citation tension, the two biases I found in my own coverage metric, and why Opus 5 never writes the anthology essay
- `docs/plans/WRITER_PROMPT_POSITIVE_EXEMPLARS.md` — the mined exemplar pool (Pss 65–71) with provenance, and the METHOD: beta-reader AHA/FELT/WIT tags as an independently-judged candidate pool, every candidate verified present in the COPY-EDITED text, RULE 13's anti-pastiche guard attached to every block. Reuse this method for the affect exemplars. **Caveat: nine of the original twenty came from Ps 71 itself and had to be replaced** — never mine exemplars from the psalm you are testing on
- `docs/plans/NEXT_SESSION_PROMPT_session_371.md` — CONSUMED (Session 371): (a) mine recent guides for great POSITIVE examples and (b) run the A/B/C test on the three Opus-5 prompt findings (delete verification scaffolding / flip negative examples to positive / add a conciseness instruction) — **both explicitly requested by the author at the end of Session 370**. Then (c) validate the untested third RULE 8b revision, (d) the carried-forward Session-369 items.
- `docs/plans/WRITER_PROMPT_RULE_8B_FINDINGS.md` — **READ BEFORE TOUCHING `RULE 8b` OR `RULE 3b-2`**: why the commentator-quoting fix was needed (the 2.2–2.6× density measurement), what the two A/B runs showed, and **the amendment that backfired** (permissive "go over budget" / "spend what you save" language rebounded length +19% and raised inert citations 4→7). Includes the canary-regex lesson: match on the *insight*, not the attribution
- `docs/plans/NEXT_SESSION_PROMPT_session_369.md` — PARTLY CONSUMED (Session 370): item (b) "make Opus 5 shorter" advanced −17% via RULE 8b, with a cheaper untried lever identified (a conciseness instruction; `effort` is documented NOT to work for this). Items (a) Opus 5 adoption, (c) colon-level doublet alignment for the Ps 71↔31 class, (d) post-copy-edit scripture verification all still carry forward
- `docs/plans/NEXT_SESSION_PROMPT_session_368.md` — CONSUMED (Session 368): item (a) Terra-as-copy-editor settled, (c) Ps 71 run; (b) Opus 5 conciseness carried into the 369 prompt
- `docs/plans/COPY_EDITOR_TERRA_FINDINGS.md` — **SETTLED, do not re-run**: copy editor pinned to `gpt-5.4`, NOT `gpt-5.6-terra`. Terra makes ~1.7× the edits on identical input and spends the difference literalizing figurative prose and "correcting" non-biblical claims from memory (once fabricating a Hebrew reading). Includes the prompt fix that backfired and 4 known-good probe cases. Read before touching `COPY_EDITOR_SYSTEM_PROMPT` or moving the copy editor back to Terra
- `docs/plans/SONNET5_MICRO_AB_FINDINGS.md` — SHELVED model swap: micro agent Sonnet 4.6 → Sonnet 5. Live A/B on Ps 65; `effort=xhigh` cheaper-but-thinner, `effort=max` richer-but-pricier-than-4.6; no clean win. Not adopted; all code reverted. Read before re-investigating a Sonnet 5 micro swap

## File Organization Rules

- **Production code:** `src/`, `main.py`, `scripts/`
- **Experimental/test:** Use during session, archive immediately after
- **Archive after 1 session:** Test scripts, debug outputs, temp files → `archive/`
- **Never commit:** `*.log`, `*_output.txt`, temp analysis files (in .gitignore)

## End-of-Session Checklist

1. **Update this file (CLAUDE.md)**: Increment session number (line 3), replace oldest of 5 recent entries
2. **Update IMPLEMENTATION_LOG.md**: Add detailed session entry at top
3. **Update scriptReferences.md**: If scripts were created or significantly changed
