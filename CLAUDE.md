# Psalms AI Commentary Pipeline

**Session**: 374 (2026-08-05)
**Phase**: Pipeline Production — tweaks and improvements

AI-powered system generating scholarly verse-by-verse commentary for all 150 Psalms using Claude (Opus 5, Opus 4.8, Sonnet 4.6), GPT (5.6 Terra, 5.1), and Gemini (3.1 Pro) with multi-agent pipeline and Hebrew concordance integration.

## Recent Work (Last 5 Sessions)
**Session 374 (2026-08-05)**: Literary echoes review — **PASS 4 HAD BEEN DELETING 40-84% OF VERIFIED ECHOES**; per-entry verification; corpus-wide author ledger; the second generator is measured and OFF
- **Trigger**: the author asked for a review of the literary-echoes components — *"we developed those a couple months ago and the world of LLMs has changed since then. Is there a way we could get better results and/or less expensive high-quality output?"* — then *"let's do all four"*, then: run Psalm 1 and compare against the pre-change run.
- **THE LLM "RECONSTRUCTION" PASS WAS SILENTLY TRUNCATING FINISHED DOSSIERS.** Counting `####` blocks in and out of Pass 4 across all 26 runs: **Ps 69 kept 4 of 25 (16%), Ps 70 7 of 19 (37%), Ps 71 9 of 19 (47%), Ps 72 11 of 18 (61%)** — every one ending **mid-quotation**. None hit `max_completion_tokens` (Ps 71 emitted 2,784 of a 32,000 budget); the model self-terminated and `finish_reason` was never checked. **Ps 71 is the psalm every writer-prompt A/B since Session 370 ran on**, so Session 372's "arm E, 49 quoted lines, highest ever" was scored against a file missing half its echoes. Overall retention 81%. **Pass 4 is deleted**: Pass 3 now returns structured per-entry verdicts and the new `src/agents/literary_echoes_parser.py` rebuilds the document by string manipulation, which cannot stop early. Pass 4's unenforceable instruction 7 ("do not substantively alter surviving ones") is now enforced by construction — **20/20 analysis paragraphs verified byte-identical** on the live run.
- **Three more silent failures in the same audit.** (a) `response.text or ""` swallowed empty Gemini generations — **3 of 26 runs lost a whole pass** (Ps 1 and 68 lost Pass 1, Ps 55 lost Pass 2); every call now validates it produced an author block, retries, then raises. (b) The module's **duplicate pricing table was stale** — terra at $2.50/$15.00 six weeks after Session 373 corrected `cost_tracker.PRICING` to $2.00/$12.00, with the docstring saying *"Keep in sync"*. (c) Reasoning tokens were **double-counted** and cached input billed as fresh — `src/utils/openai_usage.py` documents exactly this trap and literary echoes had never been migrated to it. Together, **−53%** on a representative Pass-3 call's reported cost.
- **PASS 3 IS NOW ONE CALL PER ENTRY**, fanned out over 6 threads, returning `{verdict, corrected_heading, corrected_quotation_block, reason}` keyed to a parser-assigned ID. **Every ambiguous case fails safe toward KEEPING the entry** — deliberately, since the bug being fixed was silent loss. On Ps 1 it caught a real fabrication (Anna Margolin: the poem is real, *"its full text contains neither these Yiddish lines nor this translation"*) and applied 7 precise corrections. Wall clock **178s → 74s**. **Honest note: the fan-out is NOT cheaper per entry** — batched scaled sublinearly (~6.2k input tokens/entry) vs ~12.1k per-entry; the savings come from Pass 4's removal and the pricing fixes.
- **CORPUS-WIDE AUTHOR LEDGER** replaces the 4-file mtime scan, which could only stop *adjacent* repetition while the Second Echo Principle is a claim about the whole 150-psalm series. Rebuilt from the canonical files every run; bans on recency (last 4 psalms) **and** lifetime use (≥3 psalms); accent-folded keys. Saw **289 authors** and banned 100 on Ps 1 where the old scan saw 31. **The current prompts are working, though** — across the 24 API-pipeline psalms: zero banned-author leakage, only 4 exceed the ≤2 canonical cap, max reuse 4× over 230 authors. The ugly corpus numbers are all **legacy pre-tier-override files**. The ledger is insurance for scale, not a fix for a present failure.
- **THE SECOND GENERATOR IS BUILT, MEASURED, AND OFF BY DEFAULT.** The idea works — Sonnet 5 supplied **11 authors Gemini never reached, 2 overlaps of 13** — but no Anthropic model can run the Pass-1 prompt. Nine configurations: **opus-4-8 blocked 8/8** on an output content filter (across 32k/64k, effort high/medium, a softened profanity section, and a no-lyrics restriction); sonnet-5 blocked at medium/64k; sonnet-5 at **effort=low** is the only setting that completes, and it writes its deliberation into the document (`#### Gwendolyn Brooks — not eligible (American, but let me choose properly)`). New `drop_malformed` rejects any entry lacking a quotation block or analysis, whatever model emits it. **Next thing to try, not attempted**: a non-Anthropic second generator — `gpt-5.6-terra` already has a key and cost model from Pass 3. Also: `apply_effort` was the wrong tool here (it is the source of truth for the three deep-reasoning agents) and inheriting its `high` is what starved Sonnet 5 of output room.
- **Ps 1, before vs after** (it was one of the three broken runs, so the baseline is crippled): echoes **6 → 20**, bytes 5,893 → 22,015, verses covered **4 of 6 → 6 of 6**, cost per delivered echo **$0.109 → $0.057**. Projected steady state on a healthy psalm, from re-pricing the 23 good runs: **$0.90 (−8%)**. Old artifacts preserved at `output/psalm_1/literary_echoes_PRE_S374/`.
- **Watch**: Ps 1 is **one psalm and a broken baseline** — the −8% is a projection, not a second live comparison; re-running Ps 72 would settle it. **Psalms 69-72 still hold truncated dossiers** and would need re-running to recover their lost echoes. `LIFETIME_BAN_AT = 3` has never been exercised at scale.

**Session 373 (2026-08-02/03)**: ARM E IS PRODUCTION; Hebrew bold was never specified the way Word reads it; the COPY EDITOR has been silently deleting quotation marks; commentators 7 → 6 → 11; **PRODUCTION HAD NEVER BEEN CUT OVER TO OPUS 5**; two pricing rows were wrong
- **Trigger**: the author approved arm E for production and reported four follow-ups — no bolded letters in the DOCX, remove Metzudat David everywhere ("this commentary is VERY rarely cited"), two spots where the English after a long Hebrew quotation lost its quotation mark, and three explanations that stopped one step short. Then, later: add five more commentators, two of them *"with judiciousness for a different perspective"*, and *"make sure they are listed in the methods section at the end of the DOCX."*
- **ARM E SHIPPED.** `variant_e(MASTER_WRITER_PROMPT_V4)` was verified **byte-identical to the saved arm-E template** before being applied to `master_editor.py` as one whole-string replacement (−10,186 chars). The SI pipeline inherits it (derives from V4). `variant_d`/`variant_e` are now no-ops **by construction** — both `_cut` the two `VALIDATION CHECK` blocks, which are gone, so they raise in `ab_writer_prompts.py`'s pre-flight build before any writer call is paid for. Production then diverges from the tested template by two author-requested, **un-A/B'd** deltas: Metzudat removal (−31) and the new RULE 5 (+2,755). **Deliberately left**: RULE 8/8b still reference `PHRASE COVERAGE`, which arm E deleted — those sentences were in the arm that measured well, so removing them is an untested delta. Clean it next time the prompt is varied.
- **NO DELIVERED DOCX HAS EVER CONTAINED A SINGLE `w:bCs`.** `w:b` is the LATIN bold property; for a run carrying `w:rtl` — every Hebrew run this generator emits — Word takes bold from **`w:bCs`**. The Ps 71 file had `w:rtl` 298 times and `w:bCs` **zero** times, while carrying nine correctly-split, correctly-bolded letters (`מִ | יַּ | ד`). Identical in shape to the `w:sz` vs `w:szCs` problem `_set_style_complex_size` already existed to solve; it had just never been extended to bold. Fixed by `_mirror_bold_to_complex_script()`, a post-pass (several paths set `run.bold` *after* marking a run Hebrew, so a post-pass cannot be defeated by ordering). **BOLD ONLY** — the first cut mirrored italic too and would have slanted the Hebrew inside quoted piyyutim, which Word fakes since Hebrew has no italic; `w:iCs` stays unset and Hebrew stays upright. Author confirmed *"I DO see the bolded letters now."* What remains genuinely open is whether bolded letters **read** well — 9 instances is almost no human review for RULE 3b-2's top-ranked technique.
- **THE COPY EDITOR HAS BEEN EATING QUOTATION MARKS, UNREPORTED, ACROSS ~30 PSALMS.** The author's `hoping in You is not because "` was NOT a DOCX bug: the pre-copy-edit file reads `היא ג"כ מאתך, "hoping in You…` and the post file reads `היא גכ מאתך, hoping in You…`, with no entry in the change list. **`_strip_quotes_around_source_text` treats a Hebrew abbreviation's gershayim as an opening quote** — the `"` inside ג"כ opened a span that ran through the Hebrew and closed on **the real opening quote of the English translation**, deleting both. Measured over all 114 pre-copy-edit files: **220 strips, of which 25 were not quotes** — ג"כ, מה', הקב"ה, אע"פ, ע"ד, and **Sappho's Greek elisions** in Ps 55 (`δ' ὀλίγω 'πιδεύης`). Fixed with two lookarounds over the whole source-language range **including nikud** (Ps 53's הַתִּי"ו and בְּסָמֶ"ךְ carry a vowel point right before the mark). An abbreviation mark abuts its letter; a wrapping quote never does.
- **The second reported spot was a different bug, in the DOCX generator.** `', "so Your righteousness` — `_split_long_hebrew_block`'s `heb_word` spans only `U+0590-U+05FF`, so a trailing ASCII geresh (כולם הם מה') fell outside the match and was pushed into the English, where the leading-punctuation strip could not remove it. Now absorbed back into the Hebrew block, **guarded** so a genuinely *wrapped* span keeps its closing quote outside. Regression over **28,675 paragraph lines**: behaviour changes on **2**, both improvements of the same artifact class.
- **METZUDAT DAVID REMOVED — the author's intuition was right by 7×.** Measured over the 71 production guides (dossier entries supplied → name cited): Radak 636→444 (70%), Rashi 470→365 (78%), Ibn Ezra 627→241 (38%), Malbim 452→210 (46%), Meiri 622→107 (17%), Torah Temimah 163→72 (44%), **Metzudat David 610→60 (10%)** — **third most supplied, least used**, under one citation per guide. Structural, not accidental: it is running paraphrase by design, which is exactly what RULE 8b rejects. Removing it drops ~8.6 dead glosses per psalm from the unranked dossier. Gone from the Sefaria request (the table is recorded in a comment at the point of decision), both micro-analyst templates, the research bundle's own bibliography, the synthesis writer, beta reader §5b, four sites in the writer prompt, five runners, `score_prompt_ab.py`, and five active docs. **`writer_prompt_variants.py` was edited in lockstep** — arm B's blocks are no-ops only because `_insert_before` finds them already present; changing one copy and not the other makes `variant_b` silently insert a stale duplicate.
- **FIVE COMMENTATORS ADDED (author's request) — the dossier goes from 6 sources to 11**: **Minchat Shai**, **Malbim Beur Hamilot**, **Chomat Anakh (Chida)**, **Romemot El (Alshich)**, **Metzudat Zion**. All verified live on Sefaria and coverage measured over 36 real verses before wiring: Romemot El **100%** of verses at ~763 Hebrew chars, Minchat Shai 58%/598, Metzudat Zion 50%/65, Malbim Beur Hamilot 36%/119, Chomat Anakh 19%/838 — and **none of the five carries English on Sefaria**, so the writer translates them itself. **Metzudat Zion is NOT a reversal of the Metzudat David removal** — the Metzudot are two works: David is the running paraphrase (dropped), Zion is a one-line glossary (cheap, kept). **RULE 8b now says what each source is FOR**, because ranking is impossible for a work the model cannot identify: Minchat Shai *"is not a commentary at all"* and joins Torah Temimah in the **protected** category (only source that can settle a spelling/accent/variant); Metzudat Zion is *"almost never worth a citation — use it like a dictionary, silently"*; **Romemot El is held HARDER than instinct suggests because volume is not insight**; the Chida's scarcity is a signal. The author's *"judiciousness"* for the two outsider voices is rendered as a test, not a mood: quote when they see the verse differently from everyone else, cut first when they elaborate what Radak already said. STAGE 4 closes the loophole — *"adding five sources did not raise the Tier-1 budget by one quotation; it widened the field you are ranking."*
- **The DOCX methods section names the AUTHORS** (author-requested): new `COMMENTATOR_DISPLAY`/`display_name()` in the librarian, used by both readers of `commentary_counts`, so the bibliography reads `Chomat Anakh (Chida) (5); … Romemot El (Alshich) (24)` instead of bare titles. Verified by rebuilding Ps 71. **A bug the addition would have created was caught first**: the runners' unanchored `r'### .*Malbim'` also matches `Malbim Beur Hamilot`, so every Malbim entry would have been double-counted and every DOCX bibliography silently inflated. Counting is now **derived from `COMMENTATORS` and anchored** in all five runners (which also drops the stale `Sforno` that is never fetched); verified identical to the old counts on the real Ps 71 dossier. **Cost, stated plainly**: ~+64 entries and ~32K Hebrew chars per psalm, mostly the Alshich — whether the wider field improves the guide or just gives RULE 8b more to reject is open.
- **THE CLARITY ITEM → RULE 5 NOW ASKS FOR THE STEP.** The author's three cases share one shape no existing rule catches: **v.6** Sotah 45b cited "for the head" with nothing in the quoted clause saying *head*; **v.8** Berakhot 50b's three-way ruling laid out in full but only one branch explained; **v.19** Malbim set up as natural-vs-miraculous and the verse never assigned to either. RULE 8 asks for a payoff and each paragraph *has* one; RULE 8b's admission test passes on all three. **Nothing asked the writer to show how an inference was reached.** Fixed at RULE 5 (which already owned "explain the connection" and was one unillustrated sentence — the weakest profile in the prompt by Session 371's finding): three real BROKEN/FIXED pairs, a pointer to the prompt's own Ḥullin 60a exemplar as the model of the move, and a **cut** option so it is not purely an instruction to add words. **Arm E raises the stakes** — freed from coverage duty the writer selects on interest, and interesting material is disproportionately *inferential*. **No metric**: this is semantic, a regex proxy would repeat the §7 canary mistake, and the beta reader is off with ±6 noise. The measurement is the author reading the next guide.
- **PRODUCTION HAD NEVER BEEN CUT OVER TO OPUS 5 (author-caught).** He ran Ps 72, read the methods block, and asked *"did it use Opus 5?"* — it did not: `--master-editor-model` defaulted to `claude-opus-4-8` in both runners. **This is Session 372's failure in a new place**: every writer-prompt change from Session 370 on (RULE 8b, RULE 3b-2, arm B's exemplars, arm E's translation slot, RULE 5) was designed and A/B'd on Opus 5 — `ab_writer_prompts.py` has `DEFAULT_MODEL = "claude-opus-5"` — while production sat on 4.8 for six sessions. Session 368's *"Opus 5 adoption still open (recommendation stands: adopt)"* was never actioned. **So Ps 72 is arm E's prompt on the model it was not written for, and every arm-E number the author has seen came from a model production wasn't running.** Fixed at all three sites per runner, with a comment at the argparse site so it is not "restored".
- **THE WRITER ONLY — and that took a code change, not a different default.** Author's correction: only the writer was designed for Opus 5; synthesis discovery stays 4.8 for cost. But the runners read `sd_model = master_editor_model if "claude" in ...` — **synthesis discovery FOLLOWED the writer**, so flipping the writer would have silently dragged the sidecar to Opus 5 too. Now pinned to `synthesis_discovery.DEFAULT_MODEL` with a new `--synthesis-discovery-model` escape hatch. Net: **Opus 5 writer; Opus 4.8 macro + synthesis discovery.** Macro has never been A/B'd on Opus 5 — deliberate, but it means "we're on Opus 5" is true of the writer only.
- **PRICING AUDIT — 2 of 12 rows were wrong, both live in production.** Checked every `cost_tracker.PRICING` row against the vendors' live pages on 2026-08-03. **Haiku 4.5 was $0.80/$4.00, actually $1.00/$5.00** (under-reported every citation-verifier run 20%). **GPT-5.6 Terra was $2.50/$15.00, actually $2.00/$12.00** — over-reported ~25%, and Terra is our heaviest non-Anthropic spender: the Ps 72 run reported **$1.3273 where the truth is ~$1.06**. The stale comment is the lesson — *"priced IDENTICALLY to gpt-5.4… cost-neutral by construction"* was true at Terra's GA and quietly stopped being true while the comment kept asserting it. Verified correct: Opus 5/4.8/4.7/4.6 $5/$25, Sonnet 4.6/4.5 $3/$15, GPT-5 & 5.1 $1.25/$10, GPT-5.4 $2.50/$15, Gemini 3.1 Pro $2/$12. Added a `claude-haiku-4-5` alias row and a caveat that **Gemini is tiered by prompt length** ($4/$18 above 200k input) and we encode only the cheap tier. **Every cost figure in the Session 367–373 record used the two wrong rows.**
- **What the cutover COSTS: +$0.40/psalm (~7%).** Sticker price is identical ($5/$25 for both, verified 2026-08-03) — the delta is entirely output VOLUME, because Anthropic folds thinking into billed output. Writer stage only, Ps 72, identical dossier: 4.8 = 200,441 in / 22,544 out = **$1.57**; Opus 5 = 201,221 in / 38,556 out = **$1.97** (1.71× output tokens). A full run goes ~$5.85 → ~$6.25 at corrected pricing. Session 368 measured +$0.59 on Ps 71 under the OLD prompt (1.99×), so the delta moves with the prompt — it is not a fixed model tax.
- **PS 72 RERUN ON OPUS 5 — IT WROTE 2× THE WORDS, AND THAT EXPOSES WHAT ARM E ACTUALLY DID.** Writer stage only, identical dossier, same arm-E prompt. Raw output: verses **4,159 → 8,963 (2.16×)**, total 5,648 → 11,170, **208 → 448 words per verse**; $1.97, 707s. Bigger than Session 368's 1.60× model gap — because arm E deleted `300-500 words per verse`, which Session 372 proved was the length driver. **Remove the floor and the models diverge: Opus 5 self-regulates to ~448/verse (Ps 71 arm E was ~359); 4.8 collapses to 208.** So the Ps 72 guide the author read was not just the right prompt on the wrong model — it was **about half the length it should have been**, and it is NOT evidence about arm E. Arm E's subtraction is Opus-5-specific in a way nobody had measured.
- **Commentator use split as Session 370 predicts**: verse mentions 4.8 = 18, Opus 5 = 47 — almost all of it established sources (Rashi 8→20, Malbim 3→11, Radak 3→10), not the five new ones (~6 between them, and **Romemot El drew ZERO by name** despite being supplied on all 20 verses — the Alshich-crowding worry did not materialise on its first outing). But 47 mentions on 20 verses is above Ps 71 arm E's 32 on 24, so RULE 8b's budget is under more pressure on Opus 5 here. Watch, don't act yet.
- **Deliverable**: `Documents/Psalm study guide/Psalm 72 (Opus 5).docx`, beside the existing 4.8 guide from the same dossier. Verified: methodology reads `claude-opus-5`; bibliography lists all eleven with attributions (146 entries); `w:bCs` on 299 runs, `w:iCs` 0, 5 bolded Hebrew letters that render.
- **Deliverable**: `Documents/Psalm study guide/Psalm 71 (Arm E - translation slot) v2.docx` — same commentary, rebuilt at $0 with all three formatting fixes (new name because Word held the original open). The one quotation mark the copy editor had already eaten was restored by hand against the pre-copy-edit text; it was the only damaged site in the arm. Existing guides keep their damaged marks — the fix is forward-only, and re-rendering cannot restore what was deleted from the markdown.
- **Watch**: **Ps 72 is arm E's prompt on Opus 4.8** — the writer-only Opus 5 rerun on the same dossier is the 4.8-vs-5 comparison, and arm E's fresh-psalm confirmation is only valid from the Opus 5 side; arm E's margin still rests on Psalm 71 alone (ten arms on one psalm); RULE 5 and the commentator-set prompt edits are unvalidated; **the Alshich is the one to watch** (on every verse, long, the only new source that can crowd the dossier); do bolded Hebrew letters actually read well?

**Session 372 (2026-08-02)**: The author's TRANSLATION SLOT fixes over-citation (mentions 50→32, lowest ever); the BETA READER is turned OFF after variance calibration shows its counters are noise (~$6.0)
- **Trigger**: author asked to address B4's coverage/over-citation regression and the affect item, and — the session's framing question — *"are we tripping ourselves up? are we optimally prompting for a SOTA model?"*
- **PRODUCTION WAS RUNNING THE LOSING HALF OF SESSION 371.** Arm B's six `WORKED EXAMPLES` blocks won and were never merged out of `writer_prompt_variants.py`; B4's two edits failed both halves they targeted and *were* shipped. Fixed: exemplars inserted (+10,890 chars), B4 reverted, `master_editor.py` verified **byte-identical to the saved `B3_final` template**. `variant_b` now raises rather than silently double-inserting.
- **`300-500 words per verse` is the length driver, and nobody had ever varied it.** `git log -S` dates it to Session ~130 — written for models that UNDER-produced. It is binding: across all seven Session-371 arms the median verse section is 330-437 words and **never once falls below 330**. Deleting it (arm D) gave **−918 words**; arm C's *instruction* to be concise gave **+373**. That also explains C's backfire — the instruction contradicted a word target 15K chars further down the same prompt. Coverage went UP at the same time, so length and coverage were never in tension.
- **THE AUTHOR'S TRANSLATION SLOT IS THE SESSION'S RESULT.** Print the whole verse in English, offset as a `>` block quote, under the Hebrew; coverage then holds *by construction* and the commentary selects on interest alone. Arms D and E were identical except for the slot: **commentator mentions 50→32 (lowest ever)**, Tier-1/verse 1.96→1.21, commentators/1k 5.8→3.7, grammar terms 16→9, redundancy markers 4→0, verse words 0.91×, `xlat` **24/24**, cheapest and fastest arm. Freed room went to **literary echoes (49 quoted lines, highest ever)**, not paraphrase. This confirms Session 370's coverage→citation mechanism and explains B4: the channel is structural, so words could never close it. **Needs no pipeline changes** — `document_generator.py` already renders `>` as indented italic and uses only `verse["commentary"]`; grouping keeps the guarantee (E grouped once, interleaved correctly).
- **THE BETA READER DOES NOT SURVIVE REPEATED MEASUREMENT — and is now OFF by default.** New `scripts/beta_reader_variance.py` re-read ONE FIXED TEXT 4× per arm: `UNEXPLAINED GRAMMAR` base [6,6,1,5] vs arm A [14,9,11,15]; `INERT CITATIONS` base [8,9,6,6]; `Wit` base [4,6,3,4]; `Emotional impact` base [8,6,6,7]. **Every between-arm gap of 1-3 in the Session 370-372 record is inside the judge's noise**, and **Session 371's "UNEXPLAINED GRAMMAR 11→9, arm A shows no capability regression" is INVERTED** — base averages 4.5, arm A **12.25**. Deleting the checklist really does hurt grammar; single-sampling hid it because both originals were off in opposite directions. Author's call: **turn it off** rather than pay 3× to median a judge he doesn't trust. `--beta-reader` opts back in; `--skip-beta-reader` kept as a no-op. Scorecard rows from the beta reader are now marked `†` with their measured noise band.
- **Affect: the reported regression did not exist.** D/E scored `Emotional impact` 6 against a universal 7, and `Wit` 3 — but base re-reads gave Emotional impact 6 twice and Wit 3-6. Reading all three guides end to end confirms it: E has **seven** dry-deadpan moments to base's five, and fixes two of base's own jokes (base *announces* one — "there is something faintly comic in the literalism" — which RULE 13 forbids; E's replacement is "Smart's cat needs only a spine"). **Wit is the one affect signal that survives**: arm A 6.7 vs ~4.1. `POET'S FEELING` (added this session, measurement-before-legislation) shows **no discrimination** (0-2 everywhere) — redesign or drop.
- **Deliverable**: `Documents/Psalm study guide/Psalm 71 (Arm E - translation slot).docx`. **Arm E is NOT in production** pending the author's read.
- **Watch**: nine arms have now run on Psalm 71 alone — E's margin needs one fresh-psalm confirmation before it can be trusted as general. Wonder/the poet's own feeling remains unaddressed (no exemplars mined: the register genuinely isn't in the Pss 65-70 corpus, and fabricating them would break arm B's method).

**Session 371 (2026-08-02)**: Opus-5 prompt A/B/C on Ps 71 (6 arms, $20.5) — positive exemplars WIN, conciseness instruction BACKFIRES, scaffolding-delete is a trade; word-and-phrase floor added; affect audit
- **Trigger**: author asked for the Session-370 agenda — mine recent guides for positive exemplars, then run the A/B/C test. Six writer arms, all Opus 5, one dossier, writer stage only. Full record: `docs/plans/SESSION_371_AB_RESULTS.md`.
- **A baseline arm was run first** because A/B/C are deltas from a prompt whose third RULE 8b revision had never executed; without it every delta would have been confounded against run 2. This also **settled agenda item (c): rev3 is directionally right but weak** — 9,507 verse words vs run 2's 9,846, recovering ~3.4% of the 19% the permissive amendment gave away.
- **Arm C (conciseness instruction) BACKFIRED — do not retry as written.** +373 verse words, +8% output tokens, slowest run, MOST commentator mentions (40) and quoted lines (35). The documented ~20% reduction did not replicate. **My instruction contained a protective clause** — *"cut words, never material"* — structurally identical to Session 370's *"spend what you save."* **Standing prior, now five instances** (S368 copy editor, S370 RULE 8b, S371 arms C / B2 / B3): on this prompt, added text intended to produce restraint reliably produces its opposite. Prefer demonstrations to exhortation.
- **Arm A (delete 24-item FINAL VALIDATION CHECKLIST + 2 VALIDATION CHECK blocks) is a TRADE, not a win.** +13% length and Tier-1 quotes broke to 1.21/verse (the item enforcing the budget was among the deleted) — but the "no capability regression" claim held: INERT 7→6, UNEXPLAINED GRAMMAR 11→9, LANDING found, and **wit scored 3→6 despite the WIT checklist item being deleted**. Scoped deliberately: RULE 11, PHRASE COVERAGE and RULE 8b's flat-gloss test were KEPT — rules phrased as questions, not re-read passes.
- **Arm B (positive exemplars) WON — by making the prompt 7,325 chars LONGER.** Only arm to shorten the guide (−10.5%), only one to move UNEXPLAINED GRAMMAR (11→6), Tier-1 exactly on budget, cheapest, fastest. Six `WORKED EXAMPLES` blocks on the rules that taught by prohibition alone, add-only, each with RULE 13's anti-pastiche guard. **Mechanism: prohibitions are discharged by ADDING** — "no orphaned facts" is safest satisfied by attaching a payoff to everything; a worked instance of "one honest clause" is what licenses stopping.
- **But B dropped COVERAGE to 69.9%, the worst of eight arms** — caught by a new `check_phrase_coverage.py` audit against the author's standing requirement that every word be treated at least briefly. Cause: every RULE 8 exemplar demonstrated brevity, none demonstrated the floor. **I found and fixed two biases in my own metric mid-audit** (markdown bold splitting a word, which penalised the arm that bolds most; and demanding the prefixed form, so כִּנּוֹר discussed at length scored a miss); B moved 61.7 → 63.9 → 69.9% across them. It measures Hebrew re-quotation, not conceptual treatment — the RANKING is the finding, not the absolute.
- **WORD-AND-PHRASE FLOOR shipped to production** (`master_editor.py`, rule + checklist item): the phrase is the unit of analysis, **the word is the unit of obligation**. Names the three places words go dark, ranked — the verse's **last clause** (commonest), a **word repeated** from an earlier verse (a gloss at v.5 does not cover v.17; four words discharge it), and words **swallowed by a long quotation or echo**. Plus a worked example of a whole verse swept (Ps 71:21 — four words, four handled, unequal time but no silence). Took coverage **69.9 → 78.1 → 85.2%**, best ever, while coming in SHORTER than baseline (0.96×).
- **Coverage and citation-restraint pull against each other, through a channel Session 370 had already named.** B3 hit 85.2% coverage but drove INERT CITATIONS to 8, mentions to 44, density to 4.8/1k — all worst-ever — because *"the cheapest way to make a dull phrase visible is to quote the commentator who paraphrases it."* **Fix shipped (B4, scorecard NOT yet read)**: *"COVER IN YOUR OWN WORDS — a commentator is never the instrument of coverage; coverage is a translation duty, not a research duty"* + RULE 8b's *"COVERAGE IS NEVER AN ADMISSION TICKET."*
- **THE ANTHOLOGY ESSAY — why Opus 5 never writes the author's favourite Ps 71 essay.** The idea (an old poet's anthology; v.14's vow *enacted* by the method of composition) is **in the dossier**, in the macro and as synthesis-discovery Observation 4. Two prompt guards suppressed it: *"do NOT structure your commentary around them"* and a blanket CONJECTURE-hedging rule. **Both were fixed** — the essay may now be built on the best observation, no quota, and CONJECTURE now marks the INFERENCE not the facts under it. **It did not work.** Opus 5 still put it in a hedged verse note; its essay actively frames the borrowing as a deficit (*"more than a collection of borrowed pieties"*) where 4.8 treated it as the design. **Five Opus 5 runs, zero compositional theses** — a model preference, not a suppressed instruction. The honest fix is a split-model run (4.8 essay + Opus 5 verses), untested.
- **BOLDING BUG FIXED FORWARD (author-reported).** Ps 71 v.13 bolded the stressed syllable while arguing about the -ū rhyme. Not the model and not the prompt: `micro_v2.json` is dated 2026-07-26, predating BOTH Session 369's stress rewrite and Session 370's CAPS-only change, and held **350 `**` markers** that flowed straight to the writer — contradicting RULE 2's *"bold is YOURS."* `phonetic_analyst` was already clean, so the fix went **at the point of use**: `_format_phonetic_section` strips legacy stress-bold and logs it, making artifact age irrelevant for all ~150 psalms.
- **AFFECT AUDIT (author-prompted, close of session).** Pathos is **capped, not broken** — `LANDING: found` in all six arms and `Emotional impact` is **7/10 in every single one**; the "exactly one landing" rule sets a ceiling and the author is sceptical of it. **Wit is broken and Opus-5-specific** — same RULE 13, 4.8 scores 7 and Opus 5 scores 3, *"eloquent and warm rather than deadpan"*; arm A scored 6 with the WIT checklist item deleted. **Wonder is unnamed** — and per the author's correction that means the **POET's** sense of wonder or other powerful emotion, NOT the reader's AHA (which he calls "the #1 most essential thing").
- **New tooling**: `ab_writer_prompts.py` (prompt varies, model fixed; builds and validates every arm BEFORE spending, so a stale anchor fails for free), `writer_prompt_variants.py`, `score_prompt_ab.py` (structural counts only — never wording matches, per the §7 canary lesson), `check_phrase_coverage.py`. `ab_finish_arms.py` gained `--ab-dir` and `--writer-model` (without it every prompt-arm DOCX would credit the commentary to an arm id — the Session-368 bug in a new form).
- **Deliverables**: six DOCX in `Documents/Psalm study guide/` — `Psalm 71 (Baseline)`, `(Arm A …)`, `(Arm B …)`, `(Arm C …)`, `(Arm B2 …)`, and **`Psalm 71 (final).docx`** (B3). All verified: methodology credits `claude-opus-5`, zero empty bold runs.
- **Watch**: B4's scorecard is unread; the affect work is next session's headline item; an Opus 5 vs 4.8 A/B with all the B changes is the author's own suggestion.

**Session 370 (2026-08-02)**: Opus 5's weak content traced to commentator quoting → RULE 8b + RULE 3b-2; 3 DOCX formatting bugs; stress bolding freed (~$4.6)
- **Trigger**: author read the Ps 70/71 Opus-5 guides — "roughly 5 points per verse, 3 good, 1 fine, 1 that leaves me going *why did you include this*", often "mechanically quoting a commentary that adds nothing to a simple translation."
- **Diagnosis confirmed and localized.** Verse commentary only, Opus 5 vs 4.8 on the same dossier: overall length **1.67× (Ps 71) / 1.78× (Ps 70)**, but **commentator citations 2.21× / 2.62×** — the only category that outgrows the guide's expansion in *both* psalms, and denser too (7.5 vs 5.7 per 1k words). LXX flat-or-down, Talmud flat. Opus 5 uses 47% of the dossier's 158 commentator entries; 4.8 uses 22%. Not choosing *worse* — choosing *more*: v.14 keeps Malbim's excellent hope-in-tranquility point **and** adds Rashi's bare paraphrase.
- **Mechanism**: `commentary_mode="all"` dumps all 7 commentators on every verse (~130 entries, 5.4/verse for Ps 71), unranked, no quality signal — and much of it is running paraphrase *by design* (that is what a peshat commentary is). The prompt said **"Quote liberally… traditional commentaries"** and **"Engage… incorporate these materials"** with no selection criterion, while every anti-padding rule (7b, 8, 10, 11) is scoped to the writer's **own prose** — a quotation isn't an "observation," and RULE 11's translation test is trivially passed by "they wouldn't know Radak said it." The floor was enforced per verse; the ceiling never per point. Meanwhile PHRASE COVERAGE creates demand, and the cheapest way to make a dull phrase visible is the commentator who paraphrases it.
- **RULE 8b — THE COMMENTATOR'S BURDEN** (placed after RULE 8, same logic applied to quoted material): one admission test — *does the reader read the verse differently than they did thirty seconds ago?* — **cut by default**; five categories demoted to illustrations (gate 2 flagged in-line as the leaky one); **apply the test to the gloss, not your framing verb** ("turns it upside down," "the dawn of theological consciousness"); RULE 7b extended over the sentences *around* a quote; never two commentators for one point; two tiers with **"failing the admission test is not Tier 2, it is silence"**; a **~1 Tier-1 quote per verse** budget as a *ranking* instrument (rank across the whole psalm, keep the transforming gloss over the quotable one, **protect Torah Temimah / Talmudic material**, and *"keep the insight, drop the citation — do NOT refill the room"*).
- **RULE 3b-2 — POINT, DON'T NAME** (author-reported): Ps 71 said *"the conjunction is doing all the work"* twice, once as a load-bearing essay claim, with no way for the reader to see **which letter** of וְאַתָּה was meant. Naming the category fails twice — unknown term *and* unlocatable referent. Rule assumes a working vocabulary of noun/verb/subject/object/tenses and **nothing further** (the old checklist's examples were all exotic — vocative, asyndeton, litotes — so *conjunction, particle, preposition, perfect* fell below the model's threshold). Techniques ranked: **bold the letters** → name the letter in words → contrast → define-by-showing. Bare-predicate grammar banned ("חָסִיתִי is perfect" reads as praise). Second pass added **"bolding does not license the term"** after the model bolded enthusiastically and kept the jargon anyway.
- **Two A/B runs, Ps 71, same dossier, writer-stage only.** Run 1 (both rules): verse words **9,994 → 8,251 (−17%)**, commentator mentions 75 → 34, Tier-1 quotes **3.09 → 1.41 per verse**, redundancy markers 8 → 2, bolded Hebrew 0 → 7, `LANDING` near-miss → **found**, **all 4 author-flagged duds gone**. Cost: 2 first-rank citations lost (v.9's *evidential* old age — the baseline beta reader's "sharpest single insight" — and Doeg/Ahitophel). Run 2 (ranking + protected-material amendment) recovered Doeg/Ahitophel and halved unexplained grammar (10 → 6), but **backfired on length**: "going over budget is better than losing it" + "spend what you save" read as permission, words rebounded to 9,846 and the beta reader's inert count rose **4 → 7**. Same shape as Session 368's copy-editor prompt fix. **Third revision (untested, in the prompt now)**: keep ranking + protected material, but "not optional" is a *swap* not an exemption — *"The budget is a ceiling. Nothing in this rule licenses exceeding it"* — and "spend what you save" became *"do NOT refill the room; the verse gets shorter."*
- **Beta reader now measures it, free** (`5b`/`5c`, measurement-only, no extra call): emits `INERT CITATIONS: N` (plus the three least worth their space, judged on what the commentator *says*, not how the guide introduces it) and `UNEXPLAINED GRAMMAR: N`. Its independent audit is the trustworthy signal — **my own canary regexes cried loss 4 times on content that had survived reworded** (v.4's palm-vs-hand insight kept while Malbim's name was dropped — the *ideal* outcome; v.7 richer as a three-way split; v.16 quoted in full as "might of the mighty men").
- **THREE DOCX FORMATTING BUGS, two pre-existing** (found while implementing the bolding): (1) **quote spacing** (author-reported) — three code paths set the 0.5″ indent, none touched spacing, so `Normal`'s 8pt `space_after` opened a gap between *every line* of a quoted poem; now single-spaced, 0pt within a block, 8pt only after the last line. The trap: markdown separates poem lines with **blank lines**, which the old collectors read as separate one-line blocks — `_collect_quote_block` absorbs them. (2) `_add_paragraph_with_soft_breaks` split on a literal **`\$\$`** where it needed `\*\*.*?\*\*`, so `**bold**` fell through to the italic alternative, split at the right places, and set bold **nowhere** — dead in all verse-commentary prose, latent only because nothing had ever bolded there. (3) Both `_add_nested_formatting*` split on a **bare `**` delimiter**, making each part the asterisks themselves — and `'**'` satisfies `startswith('**') and endswith('**')`, so bold landed on `part[2:-2] == ''`. **This one shipped**: Psalm 41's DOCX has **12 empty bold runs and 0 bolded stress syllables**. Every guide quoting a phonetic transcription lost its stress emphasis — re-rendering costs $0.
- **Stress bolding removed at the author's request** (`phonetic_analyst.py`): stressed syllables now **CAPS only** (`yā-SHŪ-vū`), freeing bold for the writer's own sound arguments — rhymes, sibilants, shared consonants — with worked examples in the prompt. Caps still carry all of Session 369's stress work. Tests updated (the `**` markers *were* the assertion mechanism; caps assert the same placement), 33 pass. Takes effect on the next micro run; existing `micro_v2.json` still hold `**`.
- **Deliverable**: `Documents/Psalm study guide/Psalm 71 (Opus 5) rev2.docx` (run 2's text — the *permissive* end of the range, read it as such; the third revision has never been run). Named rev2 because the previous file was locked open in Word.
- **Opus 5 prompt-length research** (author asked; checked against current guidance, not memory): the "over-prescriptive prompts reduce quality" warning is documented for **Fable 5, not Opus 5** — Session 367 recorded it correctly. For Opus 5 the guidance runs the *other* way on instruction-following. But three items land on our prompt: **(a) delete the verification scaffolding** — the one Opus-5-specific *delete*, "no capability regression," explicitly inverting standard practice, and we carry **16 checklist items + 3 VALIDATION CHECK blocks + 6 "before finalizing" instructions**; **(b) our examples are 2:1 negative** (24 WEAK/BLOATED/AVOID/NEVER vs 13 STRONG/CLEAN) where positive examples are documented to work better; **(c) conciseness is a *prompting* lever, not `effort`** (~20% measured) — directly the standing "make Opus 5 shorter" item.
- **Watch**: the third RULE 8b revision is unvalidated; `INERT CITATIONS` and `UNEXPLAINED GRAMMAR` should trend down on the next production psalm, and the bolded-Hebrew rendering needs one human look in Word.

## Quick Commands

```bash
python scripts/run_enhanced_pipeline.py 23                             # Process single psalm (synthesis-discovery sidecar ON by default, ~+$2)
python scripts/run_enhanced_pipeline.py 23 --resume                     # Resume from last step
python scripts/run_enhanced_pipeline.py 23 --skip-synthesis-discovery   # Disable the Session-347 cross-verse synthesis sidecar
python scripts/run_si_pipeline.py 19                 # Special Instruction pipeline (synthesis-discovery sidecar ON by default)
python scripts/run_literary_echoes.py 53             # Standalone literary echoes (default: regenerate). Session 374: Pass 4 removed, Pass 3 verifies per entry, rebuild is deterministic
python scripts/run_copy_editor.py 36 37 38           # Standalone copy editor
python scripts/run_beta_reader.py 58 59 60           # Standalone beta reader (reader-experience report, ~$0.08/psalm)
python scripts/run_scripture_verifier.py 41          # Standalone citation verifier
python scripts/converse_with_editor.py 21            # Chat with Master Editor
```

## Key Directories

- `src/agents/` — AI agent implementations (macro, micro, synthesis, editors, copy editor)
- `src/concordance/` — 4-layer Hebrew search system
- `database/` — SQLite databases (tanakh.db, psalm_relationships.db)
- `data/deep_research/` — Gemini Deep Research outputs
- `data/special_instructions/` — Author directive files for SI pipeline
- `output/psalm_*/` — Generated commentary (production)
- `scripts/` — Pipeline runners and utilities

## Reference Docs (read only when needed)

- `docs/session_tracking/scriptReferences.md` — All scripts with descriptions
- `docs/session_tracking/PROJECT_STATUS.md` — Pipeline phases, active features, databases
- `docs/session_tracking/IMPLEMENTATION_LOG.md` — Detailed session history (300+)
- `docs/session_tracking/FEATURE_ARCHIVE.md` — Detailed docs for completed features
- `docs/architecture/TECHNICAL_ARCHITECTURE_SUMMARY.md` — Full system architecture
- `docs/architecture/TOKEN_REDUCTION_PHASE_B.md` — Ready-to-implement token reduction tasks
- `docs/plans/DOSSIER_CACHE_KEEPALIVE_PLAN.md` — SHELVED cost-reduction plan (A1): prompt-cache the shared dossier across Synthesis Discovery → Master Writer via a keepalive ping (~$0.30–0.45/psalm); shelved over writer-reorder + model-swap fragility
- **PROMPT STATE (Session 373)**: `master_editor.py` IS arm E — verified byte-identical to `output/psalm_71/_prompt_ab/E_translation/_prompt_template.txt` before shipping, then given two author-requested, **un-A/B'd** deltas: Metzudat David removed (−31 chars) and RULE 5 rewritten to demand the inferential STEP (+2,755). Build any new arm as a delta from production — `variant_b`/`variant_d`/`variant_e` in `writer_prompt_variants.py` are now no-ops that RAISE during pre-flight, which is intended. Known dangling reference, left deliberately: RULE 8/8b still cite `PHRASE COVERAGE`, which arm E deleted
- **LITERARY ECHOES (Session 374)**: `src/agents/literary_echoes_parser.py` owns all parsing, merging, and reconstruction — pure functions, 41 unit tests, no SDK imports. **There is no Pass 4**; the final document is assembled in Python, which is why it can no longer truncate. Read the module docstring in `literary_echoes_agent.py` before changing anything there: it carries the measurement tables for the truncation bug and for the nine second-generator configurations that do not work. **Psalms 69-72 still hold dossiers truncated by the old Pass 4** and would need re-running to recover those echoes
- `docs/plans/NEXT_SESSION_PROMPT_session_372.md` — **THE CURRENT AGENDA** (unchanged by Session 373, which handled the author's post-read items instead): (a) the AFFECT work — pathos / wit / **wonder**, where "wonder" means the **POET's** powerful emotion, not the reader's AHA (author's explicit correction; read the definition table before touching anything); (b) score B4, whose scorecard was never read; (c) an Opus 5 vs 4.8 A/B with all the "B" changes, and whether the answer is a split-model run. All three set by the author at the close of Session 371
- `docs/plans/SESSION_372_TRANSLATION_SLOT_AND_JUDGE_VARIANCE.md` — **READ BEFORE TRUSTING ANY BETA-READER NUMBER, AND BEFORE ANY WRITER-PROMPT WORK**. The judge-variance calibration (4 reads of one fixed text) that invalidates most Session 370-371 affect/citation reasoning and INVERTS Session 371's arm-A grammar finding; the author's translation-slot design that dropped commentator mentions to 32, the lowest ever; and why `300-500 words per verse` was the length driver all along
- `docs/plans/SESSION_371_AB_RESULTS.md` — **READ BEFORE ANY FURTHER WRITER-PROMPT WORK**; note Session 372 corrected its arm-A `UNEXPLAINED GRAMMAR` claim (see above): the six-arm Opus-5 A/B/C on Ps 71. Positive exemplars WON while making the prompt 7,325 chars *longer*; the conciseness instruction BACKFIRED; deleting the verification scaffolding is a trade (+13% length, but no capability regression and wit 3→6). Also: the coverage↔citation tension, the two biases I found in my own coverage metric, and why Opus 5 never writes the anthology essay
- `docs/plans/WRITER_PROMPT_POSITIVE_EXEMPLARS.md` — the mined exemplar pool (Pss 65–71) with provenance, and the METHOD: beta-reader AHA/FELT/WIT tags as an independently-judged candidate pool, every candidate verified present in the COPY-EDITED text, RULE 13's anti-pastiche guard attached to every block. Reuse this method for the affect exemplars. **Caveat: nine of the original twenty came from Ps 71 itself and had to be replaced** — never mine exemplars from the psalm you are testing on
- `docs/plans/NEXT_SESSION_PROMPT_session_371.md` — CONSUMED (Session 371): (a) mine recent guides for great POSITIVE examples and (b) run the A/B/C test on the three Opus-5 prompt findings (delete verification scaffolding / flip negative examples to positive / add a conciseness instruction) — **both explicitly requested by the author at the end of Session 370**. Then (c) validate the untested third RULE 8b revision, (d) the carried-forward Session-369 items.
- `docs/plans/WRITER_PROMPT_RULE_8B_FINDINGS.md` — **READ BEFORE TOUCHING `RULE 8b` OR `RULE 3b-2`**: why the commentator-quoting fix was needed (the 2.2–2.6× density measurement), what the two A/B runs showed, and **the amendment that backfired** (permissive "go over budget" / "spend what you save" language rebounded length +19% and raised inert citations 4→7). Includes the canary-regex lesson: match on the *insight*, not the attribution
- `docs/plans/NEXT_SESSION_PROMPT_session_369.md` — PARTLY CONSUMED (Session 370): item (b) "make Opus 5 shorter" advanced −17% via RULE 8b, with a cheaper untried lever identified (a conciseness instruction; `effort` is documented NOT to work for this). Items (a) Opus 5 adoption, (c) colon-level doublet alignment for the Ps 71↔31 class, (d) post-copy-edit scripture verification all still carry forward
- `docs/plans/NEXT_SESSION_PROMPT_session_368.md` — CONSUMED (Session 368): item (a) Terra-as-copy-editor settled, (c) Ps 71 run; (b) Opus 5 conciseness carried into the 369 prompt
- `docs/plans/COPY_EDITOR_TERRA_FINDINGS.md` — **SETTLED, do not re-run**: copy editor pinned to `gpt-5.4`, NOT `gpt-5.6-terra`. Terra makes ~1.7× the edits on identical input and spends the difference literalizing figurative prose and "correcting" non-biblical claims from memory (once fabricating a Hebrew reading). Includes the prompt fix that backfired and 4 known-good probe cases. Read before touching `COPY_EDITOR_SYSTEM_PROMPT` or moving the copy editor back to Terra
- `docs/plans/SONNET5_MICRO_AB_FINDINGS.md` — SHELVED model swap: micro agent Sonnet 4.6 → Sonnet 5. Live A/B on Ps 65; `effort=xhigh` cheaper-but-thinner, `effort=max` richer-but-pricier-than-4.6; no clean win. Not adopted; all code reverted. Read before re-investigating a Sonnet 5 micro swap

## File Organization Rules

- **Production code:** `src/`, `main.py`, `scripts/`
- **Experimental/test:** Use during session, archive immediately after
- **Archive after 1 session:** Test scripts, debug outputs, temp files → `archive/`
- **Never commit:** `*.log`, `*_output.txt`, temp analysis files (in .gitignore)

## End-of-Session Checklist

1. **Update this file (CLAUDE.md)**: Increment session number (line 3), replace oldest of 5 recent entries
2. **Update IMPLEMENTATION_LOG.md**: Add detailed session entry at top
3. **Update scriptReferences.md**: If scripts were created or significantly changed
