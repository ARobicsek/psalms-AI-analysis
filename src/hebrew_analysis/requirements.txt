# Hebrew Morphological Analysis Package Requirements

# Core dependency for building cache
text-fabric>=12.0.0

# Optional dependencies for development/testing
pytest>=7.0.0
pytest-cov>=4.0.0
memory-profiler>=0.60.0

# Note: json, pathlib, logging, re are stdlib (no install needed)
